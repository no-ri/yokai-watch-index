# CLAUDE.md

このリポジトリで作業するときに最初に読む文書。

## このプロジェクト

妖怪ウォッチの「妖怪図鑑」と「アニメエピソード」を相互参照できる検索サイト。
家族向けの非公式ファンサイトで、GitHub Pages で公開する。非営利。

**仕様は `SPEC.md` にある。** 本文が仕様、`> **根拠**` の引用ブロックが理由。
実装時は本文だけ読めばよいが、**仕様に矛盾や実現不能を見つけたら、根拠を読んでから人間に相談すること**。
根拠を知らずに仕様を書き換えると、検討済みの設計判断を無音で巻き戻すことになる。

## やってはいけないこと

詳細は `SPEC.md §2`。ここでは要点だけ。

1. **画像を扱わない。** 取得も保存も表示もしない。`image` パラメータを読んだら値を捨てる
2. **取得元の原文を表示しない。** 保持はしてよいが、表示は自作の要約のみ
3. **Fandom に自動アクセスしない。** `api.php`・スクレイピング・`curl`・Playwright すべて禁止。
   `raw/fandom/*.xml` は人間が置く。**それを読むパーサだけを書く**
4. **YouTube のメタデータを永続化しない。** 保存してよいのは `video_id` と自作ラベルだけ
5. **API キーを `docs/` に置かない。** GitHub Pages の公開ルートなので閲覧者全員に見える
6. **広告・アフィリエイトを入れない**

これらは実装上の都合で緩めてはならない。困ったら人間に相談すること。

## ディレクトリ

| パス | 内容 | git |
|---|---|---|
| `raw/` | 取得元の原文。Fandom XML、YouTube レスポンス | **ignore** |
| `overrides/` | 人間が手で書く対応表。パイプラインの**入力** | commit |
| `tools/` | Phase 0 の調査スクリプト。`SPEC.md §15` の実測値の出典 | commit |
| `scripts/` | 本番パイプライン。ここが実装対象 | commit |
| `reports/` | 検証出力（unknown_params, conflicts, unmatched 等） | **ignore** |
| `docs/` | **GitHub Pages の公開ルート。** サイト一式と `docs/data/*.json` | commit |

`docs/data/` が公開データ JSON の置き場で、**CC BY-SA**（`docs/data/LICENSE` 参照）。

**`raw/` → `scripts/` → `docs/data/` → `docs/` が一方向のデータフロー。**
`docs/` が `raw/` を読むことは絶対にない。

`docs/` は開発ドキュメントの置き場ではない。GitHub Pages がブランチ配信で
`/` か `/docs` しか選べないための命名。開発ドキュメントは `SPEC.md` と
このファイルだけで、`docs/overview.md` は作らない。

## 準備済みのもの

人間側の作業は完了している。以下は既に配置されている。

```
raw/fandom/*.xml               13ファイル（4,364ページ）
raw/fandom/titles.txt          4,364行。§5.13 の件数照合に使う
raw/fandom/MANIFEST.txt        取得記録と再取得手順
overrides/unresolved_yokai.csv 82件（うち解決20件）
.env                           YT_API_KEY
tools/*.py                     調査スクリプト4本
```

**`titles.txt` を消さないこと。** パーサの取りこぼし検出に必要。

## 現在地

```
Phase 0   事実確認          ✅ 完了
Phase 1   設計判断          ✅ 完了
Phase 2   取得パイプライン   ← いまここ
Phase 2.5 A項目 検証ゲート
Phase 3   A項目の生成
Phase 4   フロントエンド・公開
```

Phase 2 のタスクは `SPEC.md §14.1`。

## 実装するときの約束

### 静かに失敗しない

**このプロジェクトで最も重要な原則。**

データソースは wiki なので、誤記・表記揺れ・欠損が実在する（実例は `SPEC.md §15.4`）。
処理できなかったものを黙って捨てると、「なぜかデータがない」という状態が永続する。

- 解決できなかったリンク → `reports/unresolved_yokai.csv`
- 未知の infobox パラメータ → `reports/unknown_params.csv`
- カテゴリと infobox の食い違い → `reports/conflicts.csv`
- 突合できなかったセグメント → `reports/unmatched.csv`

**失敗してよい。サイレント失敗は不可。**

### 件数を照合する

パーサは読めたページ数を `raw/fandom/titles.txt` の行数と照合し、**食い違ったら止まる**こと。

これは実際に踏んだバグへの対策。`<text[^>]*>(.*?)</text>` という正規表現では、
本文が空のページが `<text bytes="0" ... />` と自己閉じタグで出力された場合にマッチせず、
ページごと静かに捨てられる。実測で 4,364 件中 34 件が消えていた。
件数を照合していなければ永久に気づけない。

詳細は `SPEC.md §5.13`。

### ビルド時に制約を検証する

`docs/data/*.json` を書き出す前に、禁止キー（`plot` / `description` / `etymology_raw` /
`personality_raw` / `medallium_raw`）が含まれていないか検査し、
**あればビルドを失敗させる**。`SPEC.md §2.2` の実装装置。

人間の注意力に頼らず、機械で強制する。

## 開発フロー

- **1 PR = 1 ブランチ。** マージ後は新しいスレッドで次の作業に入る
- ブランチ名は `feat/parse-fandom` のように目的が分かる形に
- コミットメッセージは日本語でよい
- 大きな設計判断が必要になったら、実装を進める前に人間に確認する

## 環境

- 職場は Windows、個人は Mac の2環境で開発する
- **パス区切りをハードコードしない。** `pathlib.Path` を使う
- Windows の MAX_PATH 制限と、非 ASCII ファイル名に注意
- ファイル出力は UTF-8。CSV は Excel で開けるよう **UTF-8 BOM 付き**にする

## Python

- 依存は最小限に。標準ライブラリで済むものは標準ライブラリで
- 追加する場合は `requirements.txt` に記録し、理由をコミットメッセージに書く
- 型ヒントは付ける
- テストは `tests/` に置く。パーサは実データの一部を fixture にする

## 用語

| 語 | 意味 |
|---|---|
| **episode（放送回）** | Fandom の1ページ。`EP001` / `MN001`。登場妖怪リストはこの粒度 |
| **segment（小話）** | 1放送回の中のAパート・Bパート。`EP001-1`。あらすじと動画はこの粒度 |
| **妖Tube** | 妖怪ウォッチ公式 YouTube チャンネル。日本語あらすじの供給源 |
| **presence** | 登場の濃淡。`main` / `cameo` / `flashback` / `mentioned` |
| **A項目** | LLM で生成する4つの実装希望機能。`SPEC.md §11` のゲート通過が条件 |

## 迷ったら

1. `SPEC.md` の該当節を読む
2. 根拠ブロックを読む
3. それでも分からなければ人間に聞く

**仕様を勝手に解釈して進めない。** この仕様は実データの計測に基づいており、
一見不合理に見える記述にも理由がある（例: `(anime)` は剥がすが `(Shadowside)` は剥がさない）。
