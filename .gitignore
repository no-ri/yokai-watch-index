# --- 機密（最優先） ---------------------------------------------------------
# SPEC.md §7.1, §13.4 / CLAUDE.md「API キーを docs/ に置かない」
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# --- 取得元の原文（SPEC.md §2.2 / §14.3）------------------------------------
# raw/ は「保持はしてよいが表示はしない」データ。コミットしない。
raw/

# --- 検証出力（SPEC.md §5.13 / §14.3）---------------------------------------
reports/

# --- Phase 0 の中間成果物（SPEC_ISSUES_ANSWER.md §3）-------------------------
# 旧版の SPEC.md を含むため、リポジトリに2つの SPEC が存在する状態を避ける。
検討資料/

# --- OS / エディタ -----------------------------------------------------------
.DS_Store
**/.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# --- 中間生成物 -------------------------------------------------------------
# scripts/ が出力し、build_data.py が読む。公開物ではない。
build/
