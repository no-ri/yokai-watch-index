# yokai-index 仕様書

妖怪ウォッチの「妖怪図鑑」と「アニメエピソード」を相互参照できる検索サイト。

- 版: 2.1
- 更新日: 2026-08-15
- 状態: Phase 0（事実確認）および Phase 1（設計判断）完了。Phase 2（実装）着手可能
- v2.1 の変更: 着手前レビュー（`SPEC_ISSUES.md`）で判明した実データとの不整合を反映。§5.8 / §5.11 / §5.12 / §5.13 / §6 / §11.2 / §15 に修正あり
- 先行事例: `no-ri/conan-index-site`（名探偵コナン エピソード検索）

---

## この文書の読み方

**本文が仕様、`> **根拠**` で始まる引用ブロックが理由です。両者は必ず分離して記述します。**

- **Claude Code へ**: 実装時は本文（仕様）のみ読めば足ります。根拠ブロックは実装に影響しません。ただし**仕様に矛盾や実現不能を発見した場合は、根拠ブロックを読んでから人間に相談してください**。根拠を知らずに仕様を書き換えると、設計判断を無意味に巻き戻すおそれがあります。
- **人間へ**: 「なぜこうしたか」を思い出したいときは根拠ブロックだけ拾い読みしてください。仕様の詳細が不明な場合は Claude Code に問い合わせてください。

**この仕様の数値はすべて実データの計測結果です。**推測値は含みません。計測の生の結果は §15 にあります。

**§16 に決定履歴、§17 に未解決事項があります。**

---

## 1. 目的とスコープ

### 1.1 目的

子ども（および家族）が、以下の操作をできるようにする。

- 妖怪を種族・ランク・属性などで絞り込んで探す
- アニメの各話を、登場する妖怪から探す
- 「この妖怪はどの回に出た？」「この回には誰が出る？」を相互に辿る
- 見つけた話を公式YouTube（妖Tube）で再生する
- 見た話・持っているメダルを記録する

### 1.2 スコープ内

| 対象 | 範囲 |
|---|---|
| 妖怪データ | Fandom 英語 wiki に存在する全妖怪（3,414件） |
| 人間キャラクター | 同上（154件） |
| アニメ | **初代『妖怪ウォッチ』**（EP001-214）と **『妖怪ウォッチ♪』**（MN001-096） |
| 劇場版 | 一覧表示のみ（検索対象外） |

### 1.3 スコープ外（第一弾）

- 『妖怪ウォッチ シャドウサイド』（SS001-049）『Y学園』（YG001-063）のエピソード
- ゲームの攻略情報、ステータス数値
- 画像（一切扱わない）
- LLM による分類軸（§11 の検証ゲート通過時のみ実装）

> **根拠**
>
> **アニメ範囲を初代＋♪に限定した理由**: 日本語あらすじの供給源が公式YouTube「妖Tube」しかなく、その配信範囲が初代（506セグメント）と♪（190セグメント、欠番ゼロ）に偏っているため。『妖怪ウォッチ!』は12本、シャドウサイドは17本しか配信されておらず、日本語あらすじが手に入らない。範囲を広げると「半分がスカスカの図鑑」になり、子ども向けとしての品質が落ちる。密度を優先した。
>
> なお SS/YG のエピソードデータは Fandom から取得済みなので、スキーマだけ用意しておけば後から追加できる。
>
> **ゲームのステータス数値を除外した理由**: Fandom wiki のフォーラムに「掲載ステータスがデータマイニング結果と食い違う」という指摘が存在する。誤った数値を出すくらいなら載せない方がよい。また子ども向け図鑑に数値は不要。

---

## 2. 絶対制約

**この章の制約は、他のいかなる要求よりも優先する。実装上の都合で緩めてはならない。**

### 2.1 画像を一切扱わない

妖怪のイラスト、アニメのスクリーンショット、メダル画像、公式サイトの画像を、取得・保存・表示のいずれもしない。

Fandom の infobox から `image` パラメータを読んだ場合も、**値は破棄する**。

> **根拠**
>
> Fandom のライセンス表示（CC-BY-SA）は**テキストにのみ適用され、画像・動画には適用されない**。Fandom 公式のヘルプにも「画像・動画ファイルは CC-BY-SA の対象外」と明記されている。妖怪ウォッチの画像は LEVEL-5 の著作物であり、利用許諾がない。ここは判断の余地がなく、事故が起きたときの被害も最大なので、機械的に禁止する。

### 2.2 取得元の原文を表示しない

| データ | 保持 | 表示 |
|---|---|---|
| 妖Tube 説明欄のあらすじ | 一時のみ | **禁止** |
| Fandom の Plot / Personality / Appearance / Etymology / Trivia | 可 | **禁止** |
| Fandom の `{{medallium}}` 図鑑説明文 | 可 | **禁止** |
| 妖怪名・話数・放送日・種族・ランク・属性等の事実データ | 可 | 可 |
| 自作の要約・紹介文 | 可 | 可 |

**実装による強制**:

- 生データは `raw/` に置き、`.gitignore` の対象とする
- サイトが読む `docs/data/*.json` には原文フィールドを含めない
- ビルド時チェック: 出力 JSON に `plot` / `description` / `etymology_raw` / `personality_raw` / `medallium_raw` のいずれかのキーが存在したら**ビルドを失敗させる**

> **根拠**
>
> 日本の著作権法30条の4により、情報解析目的での複製は広く許容される。しかし**取り込んだ原文をそのまま画面に表示すると「享受」目的が併存し、30条の4の枠外になる**。さらに本サイトは公開されるため、30条（私的使用）の抗弁も使えない。
>
> したがって「解析のために保持するが、表示は自作の要約のみ」という線引きが唯一クリーンな設計になる。この線は人間の注意力では守れないので、ビルドチェックで機械的に強制する。
>
> 事実データ（名前・番号・ランク等）はそもそも著作物ではないため、表示制限の対象外。制限がかかるのは**散文だけ**である。

### 2.3 Fandom への自動アクセスを行わない

Fandom からのデータ取得は、**人間がブラウザで `Special:Export` を操作して XML をダウンロードする**方法のみとする。

以下は禁止:

- `api.php`（MediaWiki API）へのプログラムからのアクセス
- HTML ページのスクレイピング
- `curl` / `requests` / Playwright 等による自動取得

Claude Code は「Fandom からデータを取ってくる」コードを書いてはならない。**`raw/fandom/*.xml` は人間が置く前提**で、それを読むパーサのみを書く。

**ダウンロード済みのローカルファイルを自動処理することは制限の対象外。**禁止されているのは自動アクセスであって自動処理ではない。

> **根拠**
>
> Fandom の利用規約は、ロボット・スパイダー・スクレイパー等の自動手段によるアクセスを、書面での明示的許可なしに禁止している。さらに2025年10月版の規約では、事前の書面同意なしにコンテンツを機械学習・AIシステムの学習を含むソフトウェア開発に使用することも禁止している。
>
> 「本文は CC BY-SA だからスクレイピングしてよい」は**成立しない**。ライセンスは *コンテンツの再利用条件*、利用規約は *アクセス方法の条件* であり、別レイヤーの話である。
>
> `Special:Export` は Fandom が提供する正規のエクスポート機能であり、人間のフォーム操作なので自動アクセス禁止条項に触れない。実際に robots.txt でも `Special:` 配下はブロックされており、自動アクセスは技術的にも拒否される。
>
> なお LLM による分類（inference）が「AI/ML の開発」に当たるかは解釈が割れうる。本仕様では保守的に、**原文を表示せず、自作の要約のみを出す**という §2.2 の設計で対応する。

### 2.4 YouTube API のメタデータを永続化しない

| 永続化してよい | 永続化しない |
|---|---|
| `video_id` | `title` |
| segment_id との対応 | `description` |
| 自作の要約・キーワード | `publishedAt` / `duration` |

タイトルと説明欄は取得のたびに使い捨てる。`raw/youtube/` に一時保存してよいが、`.gitignore` の対象とし、サイトが読む JSON には含めない。

> **根拠**
>
> YouTube API の Developer Policies は、承認なしに Non-Authorized Data の統計を30日を超えて保存することを禁じている。メタデータ全般にもキャッシュ・保存の制約がかかる。
>
> 一方 video ID は保存対象外の扱いなので永続化できる。そこで「保存するのは video ID と自作ラベルだけ、表示は埋め込み/リンクに任せる」という設計にすれば、規約の制約と機能要求が両立する。
>
> なおこの制約は §2.2（原文非表示）と同じ仕組みでカバーできるため、実装上の追加コストはほぼない。

### 2.5 その他の取得禁止対象

以下からのデータ取得は行わない。

- テレビ東京公式サイト（あらすじは著作物、API なし、規約で複製禁止）
- Game8 / 神ゲー攻略 / 攻略大百科 / gamepedia.jp 等の攻略サイト（規約で無断転載・自動収集を禁止）
- ピクシブ百科事典
- YouTube のウェブページ（API のみ使用）

### 2.6 広告・アフィリエイトを設置しない

収益化を一切行わない。

> **根拠**
>
> 広告を入れると、法的評価が3点で変わる。
>
> 1. **CC BY-SA/NC の確認義務**: Fandom には CC BY-NC（非営利限定）を採用する wiki も存在する。Yo-kai Watch Wiki は現状 CC-BY-SA 表示だが、商用利用するなら個別確認が必須になる。非営利なら確認不要。
> 2. **商標の記述的使用の抗弁が弱くなる**: 非営利サイトが「妖怪ウォッチ」と書くのは対象を指し示す記述的使用。しかし収益がある場合、他社商標で集客して収益を上げる構図になり、商標的使用と評価される余地が生じる（不正競争防止法の射程に近づく）。
> 3. **紛争時の請求の重さが変わる**: 非営利なら損害の立証が困難だが、収益があると収益額が損害額の基礎になる。最悪でも「サイトを消して終わり」で済まなくなる。
>
> 将来この方針を変える場合、上記3点の再確認が必要。

### 2.7 免責

本仕様の法的整理は弁護士によるものではない。上記の制約は保守的な設計方針であり、法的助言ではない。

---

## 3. データソース

### 3.1 一覧

| ID | ソース | 取得方法 | ライセンス | 用途 |
|---|---|---|---|---|
| `fandom` | Yo-kai Watch Wiki (yokaiwatch.fandom.com) | **人間が `Special:Export`** | CC BY-SA（テキストのみ） | 妖怪マスタ、人間キャラ、エピソード、登場人物 |
| `jawiki` | ウィキペディア日本語版 | MediaWiki API（自動可） | CC BY-SA 4.0 | 話数・放送日・スタッフ |
| `yotube` | 妖怪ウォッチ公式YouTube「妖Tube」 | YouTube Data API v3 | 規約準拠（§2.4） | 日本語あらすじの原材料、動画リンク |

### 3.2 sources.yaml

各ソースについて以下を `sources.yaml` に記録し、**取得コードを書く前に必ず更新する**。

```yaml
- id: fandom
  name: Yo-kai Watch Wiki
  url: https://yokaiwatch.fandom.com/
  license: CC BY-SA (text only; images excluded)
  license_url: https://www.fandom.com/licensing
  tos_url: https://www.fandom.com/terms-of-use
  access_method: manual Special:Export (automated access prohibited by ToS)
  robots_txt: Special:* disallowed
  reviewed_at: 2026-08-15
  notes: |
    ToS prohibits robots/scrapers without written permission.
    ToS also prohibits use of content for ML/AI development without consent.
    Images are NOT covered by CC BY-SA. Never use them.
```

> **根拠**
>
> コナン版（`no-ri/conan-index-site`）で確立した運用をそのまま踏襲する。データソースの規約・robots.txt・ライセンス・取得方法・確認日を、実装前に必ず記録するというルール。
>
> 理由は2つ。①規約は変わるので、いつ時点の判断かを残さないと後から検証できない。②新しいソースを足したくなったとき、確認を飛ばして実装に入る事故を防げる。

### 3.3 妖Tube チャンネル情報とクォータ方針

```
チャンネルID           UCvudj1z5SLEkOw-Jiu63IYg
アップロードプレイリスト UUvudj1z5SLEkOw-Jiu63IYg
運営                   株式会社 indi
```

アップロードプレイリスト ID は、チャンネル ID の先頭 `UC` を `UU` に置き換えて導出する。

**`search.list` を使ってはならない。** `playlistItems.list`（1ユニット/50件）と `videos.list`（1ユニット/50件）のみ使用する。

> **根拠**
>
> `search.list` は1回100ユニット消費し、100回で1日枠（10,000ユニット）を使い切る。`UC`→`UU` 変換とプレイリスト走査なら、全1,811本の棚卸しでも約50ユニット（1日枠の0.5%）で済む。実測済み。

---

## 4. データモデル

### 4.1 yokai（妖怪マスタ）

```
yokai_id          str   Fandom ページ名（例: "Jibanyan", "Sternyan"）
name_en           str
name_ja           str   日本語名（ルビ除去済み）
name_romaji       str
tribe             str   初代系種族（8種）| unknown
tribe_ss          str   シャドウサイド系種族 | null
rank              str   E|D|C|B|A|S|SS | unknown
attribute         str   fire|water|lightning|ice|earth|wind|drain|restoration | unknown
yokai_type        str   Regular|Classic|'Merican|Boss | unknown
role              str   Fighter|Tank|Ranger|Healer | unknown  （§5.14。約79%が unknown）
foods_loved       [str]
foods_disliked    [str]
family            str   派生グループ名（例: "Jibanyan"）| null
is_family_head    bool
medallium_no      {game: no}   例: {"yw": "093", "yw2": "135", ...}
introduced_in     str   第一弾では常に null（§5.14）
categories        [str] 生カテゴリ（絞り込み用）
appears_in        [episode_id]  逆引き
intro_ja          str   自作紹介文。第一弾では null（§14）   ★§11 A2
etymology_ja      str   名前の由来。第一弾では null（§14）    ★§11 A3
data_status       str   full|partial|none
```

### 4.2 characters（人間キャラクター）

```
character_id      str   Fandom ページ名（例: "Nathan Adams"）
name_en           str
name_ja           str   日本語名（例: 天野景太）
appears_in        [episode_id]
data_status       str   full|partial|none
```

> **根拠**
>
> Fandom には人間キャラクター専用の `{{character}}` テンプレートがあり、154件が構造化されている。日本語名の充足率は94.2%。
>
> 当初は `episodes.humans` に文字列配列で持つ設計だったが、日本語名が取れることが分かったため独立テーブルにする。これにより「ケータが主役の回」「フミちゃんの回」が妖怪と同じ品質で引けるようになる。参照回数の実測は Nathan Adams（ケータ）315回、Katie Forester（フミちゃん）226回、Barnaby Bernstein（クマ）219回、Edward Archer（カンチ）216回。

### 4.3 episodes（放送回）

```
episode_id        str   Fandom ページ名（"EP001" / "MN001"）。劇場版は "M01"（§5.15）
kind              str   tv|movie
series            str   gen1|uta|shadowside|ygakuen
prefix            str   EP|MN|M|SS|YG
episode_no        int
title_ja_full     str   放送回の総題（ja.Wikipedia 由来）| null
air_date          date
staff             {}    screenplay/storyboard/director/amindirector/animation
opening           str
ending            str
yokai             [{yokai_id, presence, is_debut}]   presence: main|cameo|flashback|mentioned
humans            [{character_id, presence, is_debut}]
data_status       str   full|partial|none
```

### 4.4 segments（小話）

```
segment_id        str   "{episode_id}-{seq}" 例: "EP001-1"
episode_id        str
seq               int   1から
title_ja          str   日本語サブタイトル
title_ja_norm     str   正規化済み（突合キー）
title_en          str
yotube_no         int   妖Tube のタイトル内番号 | null
yotube_video_id   str   | null
is_recurring      bool  定期ミニコーナーか（§5.14）
synopsis_ja       str   自作要約。第一弾では null（§14）      ★§11 A1
keywords          [str] キーワード。第一弾では null（§14）   ★§11 A4
data_status       str   full|partial|none
```

> **根拠**
>
> **2層構造にした理由**: 情報の粒度が非対称なため。**あらすじはセグメント単位**で手に入る（妖Tubeは小話1本＝1動画で配信）が、**登場妖怪リストは放送回単位でしか存在しない**（Fandom の `Characters` 節はページ全体に1つで、セグメント別に分かれていない）。どちらか一方の粒度に統一すると必ず情報を失う。
>
> **実データによる裏付け**: 初代は Fandom が EP001-214（214放送回）に対し **621セグメント**、妖Tube が `#2〜621`。番号が完全に一致し、欠番114もそのまま説明できる。
>
> **♪はセグメントの性質が違う**: ♪は96放送回に対し **565セグメント**（1話あたり5.89）。ただしこれは対等なセグメントではなく、MN001 の内訳は `妖怪がいる？ / 4コマ〜さんおひさしぶりズラ！〜 / 発表！妖怪似顔絵記者会見 / 妖怪スイッチ！ / 手を洗おう / 妖怪劇場〜桃太郎〜` で、本編ストーリーは1〜2本、残りは30秒程度の定期ミニコーナー。妖Tube が配信している190本は本編部分のみと考えられる。この非対称は `is_recurring`（§5.14）で扱う。
>
> なお v2.0 では「190 ÷ 96 = 1.98」と記載していたが、これは妖Tube の本数を Fandom のセグメント数と取り違えた誤りだった。
>
> **主キーを Fandom ページ ID にした理由**: 両方のソースに存在が保証されている唯一の ID だから。妖Tube の `#N` を主キーにすると、**まだ配信されていないセグメント（初代で114個の欠番）がキーを持てない**。
>
> **segment_id を親から機械的に導出する理由**: 外部サービスの採番に依存しないため。妖Tubeが配信を止めたり番号体系を変えたりしても、内部 ID は壊れない。`yotube_no` と `yotube_video_id` はあくまで属性。
>
> **`presence` を持つ理由**: Fandom の `Characters` 節に `(cameo)` `(flashback)` `(mentioned)` 等の注記が既に付いており、登場の濃淡がそのまま取れる。コナン版では「メイン/サブ/なし」の3段階を LLM で判定したが、今回は**LLM 不要でこの軸が手に入る**。
>
> **`tribe` と `tribe_ss` を分離した理由**: 種族体系が2つ併存しており、同一妖怪が両方に属する（例: Jibanyan は Charming と Mononoke の両方）。1つの軸にマージすると「プリチー族を集めたい」という操作が壊れる。
>
> **`data_status` を持つ理由**: 欠損を隠さず明示するため。未配信セグメント、あらすじ未取得、劇場版などを一律に扱える。コナン版で `unknown` を独立集計したのと同じ思想。

### 4.5 redirects（名前解決表）

```
from              str   リダイレクト元のページ名
to                str   リダイレクト先のページ名
```

エピソード本文のリンクを正規ページ名へ解決するために使う。

> **根拠**
>
> 実測251件。`Nate` → `Nathan Adams`、`Katie` → `Katie Forester`、`Bear` → `Barnaby Bernstein`、`Eddie` → `Edward Archer` のように、**主要キャラの愛称リンクが正規名に解決される**。これを使わないと同一キャラが複数IDに分裂する。
>
> なお `Kemushi-otoko` → `List of Anime-only Yo-kai` のように、**個別ページを持たないアニメオンリー妖怪が一覧ページへ転送される**ケースもある。第一弾ではこれらを解決不能として扱う（§17 U-3）。

---

## 5. Fandom の取得とパース

### 5.1 取得手順（人間の作業）

**取得済み（2026-08-15、13ファイル、4,364ページ）。** 再取得が必要になった場合の手順を以下に残す。

```
1. Special:Export で Category:Lists of Yo-kai by Medallium Number と
   Category:Episodes を展開し、lists.xml として保存
2. extract_page_list.py で全ページ名を抽出（500行ずつのバッチに分割）
3. 各バッチをテキストエリアに貼ってエクスポート
4. check_fandom_export.py で検証 → missing_pages.txt
5. missing を貼って再取得。4に戻る（収束するまで）
```

エクスポート時の設定:

```
☑ Include only the current revision, not the full history
☑ Save as file
☐ Include templates          ← チェックしない
```

`raw/fandom/MANIFEST.txt` に「ファイル名 / 取得元カテゴリ / 取得日」を記録すること。

> **根拠**
>
> **カテゴリ分割ではなく一覧ページからの逆算にした理由**: カテゴリの網羅性に依存できない。Leadoni のようにランクカテゴリを持たない妖怪が実在するため、カテゴリをいくら網羅しても取りこぼす。一覧ページは編集者が「全部載せる」意図で作っているため網羅性が高い。
>
> **`Special:Categories` は使えない**: ABC順固定でソートできない。件数順が必要なら `Special:MostLinkedCategories`。

### 5.2 テンプレート名の揺れ

妖怪の infobox は **3通りの書き方**が混在する。

```
{{yo-kai}}   {{Yo-kai}}   {{yokai}}   ← ハイフンなしが64件以上
```

正規表現は `\{\{\s*yo-?kai\s*[|\}]` を **case-insensitive** で使うこと。

人間キャラクターは `{{character}}`、エピソードは `{{episode}}` / `{{Episode}}`。

> **根拠**
>
> 実測: Jibanyan/Orcanos/Pandle は `{{yo-kai}}`、Komasan/Leadoni は `{{Yo-kai}}`。さらに `Dummkap` `N'more` 等64件が**ハイフンなしの `{{yokai}}`** を使っている。
>
> case-sensitive かつハイフン必須で書くと、3,414件のうち64件以上を静かに取りこぼす。

### 5.3 テンプレートの切り出し方

`{{yo-kai|...}}` の中身は、`{{` と `}}` の対応を数えて切り出す。**正規表現の非貪欲マッチ（`\{\{yo-kai\|(.*?)\n\}\}`）を使ってはならない。**

パラメータの分割も同様に、ネストした `{{ }}` と `[[ ]]` の内側の `|` を区切りとして扱わないこと。

> **根拠**
>
> 開発中に実際に踏んだバグ。非貪欲マッチだとネストしたテンプレートを越えて本文の散文まで取り込み、パラメータ種類数が実際の140に対して1,835と誤検出された。`yo-kai watch blasters]]''` のような散文断片がパラメータ名として現れたら、この失敗を疑うこと。

### 5.4 分類軸の決定順

種族・ランク・属性・役割・好物のすべてについて、以下の順で値を決定する。

```
1. カテゴリ（例: Category:Rank S Yo-kai）
2. infobox の該当パラメータ
3. どちらもなければ unknown

両方あって食い違う場合:
  → conflicts.csv に出力し、カテゴリを暫定採用
```

> **根拠**
>
> **どちらか一方では必ず欠損する**ことを実測で確認した。
>
> - Sternyan: infobox の `rank-ww` が空 → カテゴリに `Rank S Yo-kai` あり
> - Leadoni: infobox に `rank = E` `attribute = Ice` あり → カテゴリに rank も attribute も**なし**
>
> つまり整備状況が逆転するケースが両方向に存在する。マージが必須。
>
> **衝突を可視化する理由**: この wiki にはデータ誤りが実在する（§15.4）。黙って片方を採ると誤りに気づけない。

### 5.5 infobox パラメータのエイリアス

パラメータ名が統一されておらず、**wiki 側の誤記も混在する**。エイリアス表を持つこと。

```
rank        : rank, rank-yk, rank-yk2, rank-yk3, rank-yk4,
              rank-ww, rank-yww, rank-ykb, rank-ykb2,
              rank-kunitori, rank-sangokushi, rank-w, rank-ywmw, rank-yw3
tribe       : tribe, triobe(誤記)
tribe_ss    : sstribe
attribute   : attribute, attribue(誤記)
name_ja     : japanese name, japanese, yo-kai name, character name
name_romaji : romaji name, romaji, romanji name(誤記)
food        : food, foodyw, foodyw1, foodyw2, foodyw3, foodyw4,
              foodyws, foodywww, foodyww, foodywb, foodyb, foodspinoff,
              foosyw3(誤記)
badfood     : badfood, despised food
defence     : defence, defense, stat defence, stat defense
va_ja       : japaneseva, seiyu, voice, jva, apaneseva(誤記)
medallium_no: yw, yw2, yw3, yw4, yws, yw s(誤記), ywb, ywb2, ywl,
              yww, ywww, yskw, yg, wibwob, wibwobx, ukiukipedia
```

**実測は140種類。** 上記に載っていないものは `unknown_params.csv` に出力し、人間が確認できるようにすること。

出現回数が1〜2回のパラメータ（`icaros` `gotekki mark ii` `kabila (fragment)` 等）は特定キャラ専用なので、**無視してよい**。

> **根拠**
>
> 全3,414件のパース結果から実測。当初サンプル5件から推定したエイリアス表には typo が含まれておらず、そのままでは静かに欠損する。
>
> 誤記は5種類確認できた（`attribue` `triobe` `romanji name` `apaneseva` `foosyw3`）。いずれも1〜22件と少数だが、放置すると原因の分からない欠損になる。

### 5.6 値の正規化

infobox の値は生テキストではない。以下を除去・展開する。

| 対象 | 例 |
|---|---|
| ルビ HTML | `みちび<ruby>鬼<rt>き</rt></ruby>` → `みちび鬼` |
| `<br>` の表記ゆれ | `<br>` `<br >` `<br/>` |
| 注記付き値 | `204 (Friend)<br>#B-028 (Boss)` → 分割して保持 |
| テンプレート | `Seafood {{small|(YW)}}` → `Seafood (YW)` |
| **特殊空白** | **U+00A0 / U+3000 / U+2002-200B / U+FEFF / タブ → U+0020** |

> **根拠**
>
> **特殊空白の正規化が必須な理由**: 実データに**ノーブレークスペース（U+00A0）が混入している**。
> ```
> リンク側:  'Konbu-san\u00a0(Shadowside)'
> ページ名:  'Konbu-san (Shadowside)'
> ```
> 見た目が完全に同じなので、目視では絶対に気づけない。82件の未解決リンクのうち2件がこれだった。全459話のパースではさらに増える。

### 5.7 日本語名の取得優先順位

```
1. {{language}} テンプレートの ja-name       ← ルビなしの綺麗な版
2. infobox の japanese name                  ← ルビ HTML が混入しうる
3. {{translation}} の第2引数
4. なければ unknown
```

> **根拠**
>
> `{{language}}` の `ja-name` はルビ HTML を含まない形で書かれている（実測: `みちび鬼 ''Michibiki''`）。一方 infobox の `japanese name` にはルビ HTML が入る。優先順位をこの順にすることで、取れる範囲ではルビ問題を回避できる。
>
> **ただし `{{language}}` の充足率は 783 / 3,414 = 22.9% しかない**。残り 77.1% は infobox 経由になるため、**§5.6 のルビ除去が主経路**である。ここを軽く見ると日本語名の77%が壊れる。
>
> 日本語名が3箇所に冗長に存在するため、1つ欠けても他で拾える。最終的な充足率は妖怪 100.0%（3,414/3,414）。

### 5.8 Characters 節のパース

```
=== Humans ===  / === Yo-kai ===  の配下を対象とする

[[Jibanyan (anime)|Jibanyan]]   → Jibanyan     （(anime) は剥がす）
[[Venoct (Shadowside)]]         → そのまま      （(Shadowside) は剥がさない）
[[Noway|Noways]]                 → Noway         （パイプ左を採用）
[[Foo#Bar]]                      → Foo           （#以降を落とす）
[[Jibanyan]]/[[Sternyan]]        → 2件に分割
[[:Category:Gemnyans]]           → 破棄          （先頭コロンはカテゴリ等）
[[w:c:inazuma-eleven:Foo]]       → 破棄          （インターウィキ）
* Classic Yo-kai Trio            → 破棄          （リンクなし行）
** [[Boyclops]]                  → 採用          （ネストを平坦化）

正規化後、§5.6 の空白正規化を適用し、§5.9 の redirects を引く。

注記の解釈（実測65種類。以下の規則で畳む）:

  0) 区切り文字は ; と , の両方。複数トークンに分割する

  1) debut / anime debut / full debut は presence ではなく
     別フラグ is_debut = true として分離する

  2) 残りのトークンを次の優先順で1つに畳む（弱いものが勝つ）
       mentioned > flashback > cameo > main

  3) 実体を伴わない表現は cameo に寄せる
       medal only / silhouette only / voice only / voice-only /
       pictured / shadow cameo / face cameo / on tv / costume /
       non-speaking cameo

  4) トークンが1つも該当しない（未知の注記）
       → presence = main とし、必ず reports/unknown_presence.csv に出力する

  例:
    （注記なし）            → main
    (cameo)                → cameo
    (debut)                → main,  is_debut = true
    (debut, cameo)         → cameo, is_debut = true
    (cameo; pictured)      → cameo
    (flashback; cameo)     → flashback
    (non-speaking cameo)   → cameo
    (medal only)           → cameo
```

`Humans` 節のリンクは `characters` テーブルへ、`Yo-kai` 節のリンクは `yokai` テーブルへ解決する。

解決できない場合、**捨てずに `unresolved_yokai.csv` に出力**する。

> **根拠**
>
> **`(anime)` は剥がすが `(Shadowside)` は剥がさない**: `(anime)` は同一キャラのアニメ版ページで、統合するのが §16 の決定 D-3。一方 `(Shadowside)` は別作品の別キャラクター（`Venoct` と `Venoct (Shadowside)` は別デザイン・別設定）であり、剥がすと実在しないページ名になって解決に失敗する。開発中に実際に踏んだ誤り。
>
> **`(anime)` サフィックスは時期で揺れる**: 初代前半の話には付き（EP001）、後期や♪シリーズには付かない（EP151, MN001）。剥がした結果と剥がさない結果の両方を試すフォールバックが必要。
>
> **注記が65種類ある**: 初代＋♪の全310話を走査した実測（§15.6）。当初は6種類と想定していたが実際は65種類あった。
>
> **`(debut)` を分離する理由**: 出現数410件で `(cameo)` の1,240件に次ぐ第2位だが、これは「登場の濃淡」ではなく「初登場」を意味する。他の注記と同じ軸に混ぜると、**410件のメイン登場が誤って cameo に分類される**。別フラグにするのが正しい。しかも `is_debut` は「この妖怪が初めて出た回」という有用な検索軸になる。
>
> **未知を main に倒す理由**: どちらに倒しても誤りは残る。`main` に倒せば「登場している」という事実は保たれ、UI 上は多めに出るだけで済む。`cameo` に倒すと本来メインの回が埋もれる。**寄せた結果が後から見えること**（レポート出力）を精度より優先する。

### 5.9 リダイレクトによる名前解決

`#REDIRECT [[Target]]` を検出して `redirects` テーブルを作り、リンク解決に使う。**最大5段まで辿り、循環を検出したら打ち切る。**

> **根拠**
>
> 実測251件。これを使わないと `Nate` と `Nathan Adams` が別IDに分裂する。エピソード本文では愛称リンクが多用されている。

### 5.10 手動オーバーライド

解決不能なリンクは `overrides/unresolved_yokai.csv` の対応表で解決する。

```
from,to,confidence,note,candidate1,candidate2,candidate3
Dismerelda,Dismarelda,high,綴り誤り（a/e）。同一妖怪と判断,,,
Sighborg S,,none,Sighborg Y は別個体。誤記と断定できない,Sighborg Y,,
Hironori Yokobun,,none,解決不能（該当ページなし）,,,
```

`to` が空欄のものは「解決不能」として記録し、削除しない。

**自動での曖昧一致（difflib 等）で寄せてはならない。** 候補の提示は可、自動適用は不可。

> **根拠**
>
> 実測82件のうち、解決できたのは20件のみ（§15.5）。残りは wiki 内の赤リンクと、個別ページを持たないアニメオンリー妖怪であり、追加取得を試みても取れないことを確認済み。
>
> **自動適用を禁じる理由**: 実例として `Sighborg S` は類似度0.93以上で `Sighborg Y` にマッチするが、妖怪ウォッチには型番違いの派生（S型・Y型）が実在するパターンがあり、**統合すると別妖怪が消える**。同様に `Masachika Mitsumatagi` と `Nozuchika Mitsumatagi` は同姓の別人物。機械の類似度では判断できない。
>
> **カバレッジの記録として残す理由**: 誰かが wiki にページを作れば後で解決する。削除すると欠損の実態が追えなくなる。

### 5.11 エピソードのシリーズ判別

**`prefix` パラメータとページ名パターンの二重チェック**で判別する。**カテゴリで判別してはならない。**

実測（全459件）:

| prefix | 件数 | シリーズ | スコープ |
|---|---|---|---|
| （なし） | 216 | 初代 214 ＋ **EX011 / EX012 の2件** | 214件のみ対象 |
| `MN` | 96 | 妖怪ウォッチ♪ | 対象 |
| `SS` | 49 | シャドウサイド | 外 |
| `YSH` | 64 | Y学園（ページ名は `YG001` 形式） | 外 |
| `EX` | 34 | 妖怪ウォッチ! と推定 | 外 |

判定規則:

```
ページ名が EP\d{3} かつ prefix なし   → gen1（初代）
ページ名が MN\d{3} かつ prefix = MN   → uta（♪）
上記以外                              → スコープ外
両者が食い違う                        → reports/conflicts.csv に出力して除外
```

**`prefix = M`（劇場版）は存在しない。** 劇場版ページは export に1件も含まれていないため、
§9.4 の一覧表示は `overrides/movies.csv` から構成する（§5.15）。

> **根拠**
>
> **カテゴリでは判別できない**: MN001（♪の第1話）は `Category:Season 1 episodes` に属しており、これは EP001（初代の第1話）と**同じカテゴリ名**である。カテゴリだけで判別すると♪の話が初代に混入する。
>
> **二重チェックが必要な理由**: `EX011` / `EX012` の2件は `prefix` パラメータを持たない。「prefix なし → 初代」という単純な規則では、**スコープ外の2話が初代に混入する**。ページ名パターンを併用すれば自動的に弾ける。
>
> **Y学園の prefix は `YG` ではなく `YSH`**: ページ名は `YG001` 形式だが、`prefix` パラメータの値は `YSH`。`YG` で除外しようとすると64件を除外し損ねる。

### 5.12 日本語サブタイトルの取得

`{{translation|英語タイトル|日本語タイトル|ローマ字}}` の**第2引数**を使う。

**第3引数（ローマ字）は使用しない。**

**抽出範囲は lead 段落（最初の `==` 見出しより前）に限定する。**

セグメント数は `{{translation}}` の個数を正とする。`episode title` パラメータの
英語タイトルは `<br>` 区切りで並び、**出現順で**対応させる（文字列一致は使わない）。

個数が一致しない場合、`title_en` を null にして処理を継続し、
`reports/segment_title_mismatch.csv` に出力する。

> **根拠**
>
> **lead 段落に限定する理由**: ページ全体で数えると EP001 は4個ヒットするが、実際のセグメントは2個（`妖怪がいる!` と `恐怖の交差点`）。残り2個は後方の節にある。限定しないと全310話でセグメント数が水増しされる。
>
> **文字列一致を使わない理由**: EP151 では `episode title` が `Tomnyan's Japon Exploration: New Year`、`{{translation}}` の第1引数が `Tomnyan's Japon Exploration: First New Year` と**文字列が異なる**。出現順で対応させるしかない。
>
> **個数不一致は3件**: EP031（`episode title` パラメータ自体が無い）、EP063（同）、EP146（3対4）。処理を止めるほどの件数ではないので、`title_en` を落として継続する。日本語サブタイトルさえ取れれば `segments` は成立する。

> **第3引数を使わない理由**: 実データに誤りが存在する。EP151 の第1セグメントは:
> ```
> {{translation|'''Tomnyan's Japon Exploration: First New Year'''
> |トムニャンのジャポン探訪『はじめてのお正月』
> |Jibanyan to itsutsu no purezento da nyan!}}
> ```
> ローマ字欄が「ジバニャンと5つのプレゼントだニャン！」という**別の話のデータ**になっている。第3引数は信用できない。

### 5.13 パース時の自己検証

パーサは以下を必ず出力し、想定と食い違ったら**警告を出して止まる**こと。

```
1. ページタイトルの集合を raw/fandom/titles.txt と照合する
   - XML 内の <page> は 4,369 件だが、4タイトルが重複エクスポートされている
   - **タイトルで重複排除してから**集合として比較すること
   - 集合差を両方向で出す（titles.txt にあって XML にない / その逆）
   - <text ... /> 自己閉じタグ（実測34件）を空本文として扱わないと差が出る
2. 種別ごとの件数（yokai / character / episode / redirect /
   unclassified / namespace_skipped）
   - File: / Category: 名前空間は分類前に除外し namespace_skipped に集計する
3. エピソードの prefix 別の内訳と欠番リスト
4. unknown_params.csv            未知のパラメータ名
5. conflicts.csv                 カテゴリと infobox の食い違い
6. unresolved_yokai.csv          解決できなかったリンク
7. unknown_presence.csv          未知の presence 注記（§5.8）
8. segment_title_mismatch.csv    セグメント数と episode title の不一致（§5.12）
9. unmatched.csv                 妖Tube と突合できなかったセグメント（§8.2）
```

> **根拠**
>
> **照合が必須な理由**: 開発中に実際に踏んだバグ。`<text[^>]*>(.*?)</text>` という正規表現では、本文が空のページが `<text bytes="0" ... />` と自己閉じタグで出力された場合にマッチせず、**ページごと静かに捨てられる**。実測で34件が消えていた。照合していなければ永久に気づけない。
>
> **単純な件数比較では正常データで失敗する**: XML 内の `<page>` は 4,369 件、`titles.txt` は 4,364 行。差の5件は再取得バッチでの重複エクスポート（`Kaito Hotei` 他4タイトル、うち1件は3回出現）。タイトルで重複排除すれば集合が完全一致し、両方向の差はゼロになる。**件数ではなく集合で照合すること。**
>
> **失敗リストを出す理由**: 静かに欠損させないこと。何が取れていないかが見えていれば後で埋められるが、見えていなければ「なぜかデータがない」という状態が永続する。

### 5.14 派生フィールドの導出規則

§4.1 の以下のフィールドは、infobox にも §5.4 の決定順にも規則がないため、ここで定義する。

| フィールド | 導出元 | 取れない場合 |
|---|---|---|
| `role` | カテゴリのみ（`Fighter Role Yo-kai` 等） | `unknown`（約79%） |
| `yokai_type` | infobox の `type` パラメータ（1,104件） | `unknown` |
| `family` | `〜's Variants` カテゴリ（5カテゴリのみ） | `null` |
| `is_family_head` | `family == yokai_id` なら true | false |
| `introduced_in` | **第一弾では実装しない** | 常に `null` |
| `is_recurring`（segments） | 同一 `title_ja_norm` が5話以上に登場 | false |
| `is_debut`（episodes.yokai[]） | §5.8 の注記から分離 | false |

`role` の enum から **`Blaster` を削除する**（カテゴリ0件で取得不能）。

> **根拠**
>
> **`role` が21%しか取れない**: 実測は Fighter 275 / Ranger 169 / Healer 146 / Tank 135 の計725件（3,414件中）。`role` という infobox パラメータは存在せず、カテゴリからしか取れない。79%が `unknown` になるため、§9.1 の絞り込み軸からは外し、詳細表示のみに使う。
>
> **`introduced_in` を諦める理由**: 「初登場作品」に対応する取得元が infobox にもカテゴリにも特定できなかった。作品限定カテゴリ（`Exclusive To 〜`）は「その作品にしか出ない」であって「初登場」ではない。推測で埋めるより `null` で「データなし」と表示する方が誠実（§12.5）。
>
> **`is_recurring` を導入する理由**: ♪シリーズは1放送回あたり約5.9セグメントあるが、その多くは「手を洗おう」「4コマ」「妖怪劇場」のような定期ミニコーナーで、妖Tube では配信されない。コーナーはタイトルが毎回同じ、本編は毎回違うため、**同一タイトルの出現回数で機械的に識別できる**。これがないと♪の画面が「動画なし」だらけになる。

### 5.15 劇場版の扱い

劇場版のページは export に**1件も存在しない**（`M01` 形式 0件、`prefix = M` 0件）。

`overrides/movies.csv` に人間が手入力し、そこから `episodes`（`kind = movie`）を構成する。

```csv
episode_id,title_ja,year,data_status
M01,妖怪ウォッチ 誕生の秘密だニャン！,2014,none
M02,...
```

`data_status = none` として一覧にのみ表示し、検索対象からは外す（§9.4）。

> **根拠**
>
> §9.4 の意図は「どのような映画が存在するかを知ること自体が目的」であり、あらすじも登場妖怪も不要。8行の手入力で満たせる。Fandom を再エクスポートする（§2.3 により人間の手作業）より確実で早い。

---

## 6. ja.Wikipedia の取得

**第一弾では使用しない。**

`air_date` と `staff` は Fandom の `{{episode}}` から取得する。

```
| japan        = January 8, 2014        → air_date（英語日付形式をパース）
| screenplay   = 加藤陽一 ''Yoichi Kato''
| storyboard   = ウシロシンジ ''Ushiro Shinji''
| director     = 駒屋健一郎 ''Komaya Kenchiro''
| amindirector = 松坂定俊 ''Sadatoshi Matsuzaka''
| animation    = Team Inoue
```

`title_ja_full`（放送回の総題）は**第一弾では常に null** とする。

将来 ja.Wikipedia を使う場合は、MediaWiki API を使用する（自動アクセス可、User-Agent に連絡先を含めること）。

> **根拠**
>
> **当初「ここからしか取れない」としていたのは誤りだった**。着手前レビューの実測で、`{{episode}}` の `japan` パラメータが **310/310（100%）** で存在することが判明した。スタッフ情報も infobox にある。
>
> **`title_ja_full` を第一弾で諦める理由**: §9.2 の検索軸に含まれておらず、欠けても機能が成立する。また妖怪ウォッチのエピソードに「総題」という概念が存在するかも未確認。未検証の外部依存を1つ減らす方が、通しで走らせる第一弾には適している。
>
> これにより §17 U-1（劇場版の日本語タイトル）も ja.Wikipedia 依存ではなくなり、§5.15 の手入力で完結する。

---

## 7. 妖Tube の取得（YouTube Data API v3）

### 7.1 API 利用方針

§3.3 のクォータ方針に従う。API キーは `.env` に置き、`.gitignore` の対象とする。**フロントエンドのコードに含めない**（§10.1）。

### 7.2 ノイズの除外

以下は本編セグメントではないため除外する。

```
除外条件:
  - タイトルまたは説明欄に "#shorts" を含む
  - タイトルに "まとめ" を含む
  - タイトルが "【公式】" で始まらない
```

実測: 全1,811本のうち、この条件を通るのは858本。

### 7.3 あらすじブロックの抽出

説明欄から、`【サブタイトル】` に続く本文を抽出する。

```
正規表現の目安: 【([^】]{2,40})】\s*\n+(.{60,})
```

説明欄の先頭行は定型文（「アニメ過去シリーズから毎週◯曜に配信中！」）なので、これを本文と誤認しないこと。定型文だけで約330文字あるため、**文字数だけでのあらすじ有無判定は無効**。

> **根拠**
>
> 当初「説明欄80文字以上」であらすじの有無を測ったところ99.3%となったが、これは誤りだった。定型文（SNSリンク等）だけで330文字あるため。`【...】` ブロックの有無で測り直したところ、実際は **609/858 = 71%** だった。

### 7.4 話数番号の解釈

```
初代: 【公式】妖怪ウォッチ #N     N は 2〜621（506本、欠番114）
♪  : 【公式】妖怪ウォッチ♪ #N    N は 1〜190（190本、欠番0）
```

`#` は半角と全角（`＃`）の両方が使われる。両方に対応すること。

**妖Tube の `#N` はセグメント単位の番号であり、放送回の番号ではない。**

> **根拠**
>
> 初代は全214話だが `#N` は621まである。214話 × 各回2〜3セグメント ≒ 600 で整合する。実際 `#1` は「妖怪がいる！」で、これは EP001 の A パートのみ。
>
> これは**好都合**である。Fandom も `{{translation}}` でセグメント単位に日本語サブタイトルを持つため、**両者の粒度が一致する**。ja.Wikipedia の「第N話」だけが上位の括りになる、素直な2層構造に収まる。

---

## 8. 正規化と突合

### 8.1 文字列正規化

`title_ja_norm` は Fandom と妖Tube を繋ぐ唯一のキーである。以下をすべて適用する。

| 対象 | 例 |
|---|---|
| 記号の全半角統一 | `妖怪がいる!` ⇔ `妖怪がいる！` |
| 波ダッシュの統一 | `〜` ⇔ `～` |
| ルビ HTML の除去 | `<ruby>坊<rt>ぼう</rt></ruby>` → `坊` |
| **特殊空白の正規化**（§5.6） | U+00A0 / U+3000 等 → U+0020 |
| 空白の除去（全半角とも） | `#186 激闘！` / `#16  母熊` |
| 括弧類の除去 | `「妖怪しょうぶし」` → `妖怪しょうぶし` |

> **根拠**
>
> 実データで1話目から衝突する。Fandom EP001 は `妖怪がいる!`（半角）、妖Tube #1 は `妖怪がいる！`（全角）。正規化なしでは最初のレコードから突合に失敗する。

### 8.2 突合アルゴリズム

3段構えで実行する。

```
段1: 正規化後の完全一致
段2: difflib.SequenceMatcher、cutoff = 0.85
段3: 番号の単調性による位置補間
     （妖Tube の #N は放送順。前後の確定アンカーの間に挟まれる
       セグメントは位置から推定）
```

突合に使う文字列は、妖Tube側では**説明欄の `【...】` を第一候補、動画タイトルを第二候補**とし、両方を試す。

**突合失敗を必ず `unmatched.csv` に出力する。** 手動補正の入口として必須。

手動補正は `overrides/segment_youtube_map.csv` に記録し、パイプラインの再実行で失われないようにする。

> **根拠**
>
> **cutoff 0.85 の根拠**: specdiff の開発でマーカー文字列（10〜15文字）に対して詰めた値。日本語サブタイトルも同程度の長さなので流用できる。
>
> **タイトルと説明欄の両方を使う理由**: 同一動画内で表記が揺れている実例がある。タイトルが `妖怪しょうぶし`、説明欄が `妖怪しょうブシ`。
>
> **失敗リストが必須な理由**: 初代には114個の欠番があり、突合失敗は必ず発生する。失敗を握り潰すと、どのデータが欠けているか永久に分からなくなる。

---

## 9. 検索軸（第一弾）

### 9.1 図鑑タブ

```
種族（初代系8種）/ ランク（E〜S）/ 属性
色 / 動物種 / 好物 / 嫌いな物
ボス / レア / ファミリー
フリーワード（日本語名・英語名）
```

**軸の値は `yokai.categories`（生カテゴリ配列）からフロントエンド側で導出する。**
専用フィールドは追加しない。カテゴリ名と日本語ラベルの対応表を
`docs/data/facets.json` に持ち、UI はこれを読んで絞り込みパネルを組み立てる。

```json
{ "color":  { "Red Yo-kai": "赤", "Blue Yo-kai": "青" },
  "animal": { "Cat Yo-kai": "ネコ", "Dog Yo-kai": "イヌ" },
  "boss":   { "Boss Yo-kai": "ボス" },
  "rare":   { "Rare Yo-kai": "レア" } }
```

UI に出さないもの:

- `tribe_ss`（シャドウサイド系種族）— データとしては保持する
- `role` — 約79%が `unknown` のため絞り込みには使わない。詳細表示のみ（§5.14）
- `introduced_in` — 第一弾では常に null（§5.14）

### 9.2 アニメタブ

```
シリーズ（初代 / ♪）/ 放送年
登場妖怪（放送回単位・presence フィルタ付き）
登場人物（同上）
サブタイトル フリーワード
未視聴のみ表示
```

### 9.3 相互参照

```
妖怪   → 登場した回の一覧
人物   → 登場した回の一覧
回     → 出てくる妖怪・人物の一覧
```

### 9.4 劇場版

`kind = movie` は検索対象外。一覧の末尾に「劇場版」セクションとして**常時表示**し、「データなし」バッジを付ける。

劇場版のデータは `overrides/movies.csv` から構成する（§5.15）。Fandom の export に劇場版ページは含まれていない。

「劇場版を除く」フィルタを用意し、これを有効にしたときのみ非表示。

> **根拠**
>
> **LLM 分類軸を第一弾から外した理由**: カテゴリ由来だけで20軸以上あり、子どもの用途には飽和する。一方 LLM 軸を入れると検証工程（Gold set・混同行列・再現率測定）が丸ごと乗り、週9時間の予算では数週間が消える。**後からフィールドを追加するだけで足せる**ため、第一弾を出してから子どもの反応を見て決める方が軸の選定を外さない。
>
> **`tribe_ss` を保持だけする理由**: 取得時に一緒に取れるものを捨てる理由がない。後で欲しくなったときに再取得が不要になる。実装コストはほぼゼロ。
>
> **軸を `categories` から導出する理由**: 専用フィールドを持つと `yokai.json` が 3,414件 × 5フィールド分ふくらむ。カテゴリ配列は既に持っているので重複になる。また後から軸を足すときに**再ビルドが不要**になり、§9 の「軸は後から足せる」という設計思想と一致する。実測でカテゴリ側の材料は十分（`Red Yo-kai` 1,099 / `Cat Yo-kai` 366 / `Boss Yo-kai` 527 / `Rare Yo-kai` 294）。
>
> **劇場版を常時表示する理由**: 「どのような映画が存在するか」を知ること自体が目的。検索から外すのは、あらすじも妖Tube動画もないため。

---

## 10. フロントエンド

### 10.1 構成

- 静的サイト。GitHub Pages でホスティング
- ビルド成果物として `docs/data/*.json` を配置し、フロントは fetch して読む
- **API キーをフロントエンドのコードに含めない**（GitHub Pages は静的配信のため、閲覧者全員に見える）
- YouTube API を叩くのはローカルまたは GitHub Actions のみ

### 10.2 個人データ

視聴履歴・所持メダルは **localStorage のみ**に保存する。サーバー送信しない。

書き出し・読み込み機能を用意する（コナン版と同様）。

### 10.3 PWA

`manifest.json` と `apple-touch-icon` を配置し、iPhone のホーム画面に追加できるようにする。

- iOS には `beforeinstallprompt` がないため、インストールバナーは出せない。ユーザーが Safari の共有 →「ホーム画面に追加」を手動で行う
- `apple-touch-icon` は `manifest.json` の `icons` と併記する（旧 iOS 対策）

### 10.4 画像の代替表現

種族・属性・ランクは **CSS で描いた図形と色**で表現する。配色は自作の意匠であり、事実データではないため権利上の問題がない。

### 10.5 YouTube リンク

```
https://www.youtube.com/watch?v={video_id}
```

- 埋め込みプレイヤーではなく**外部リンク**を使う
- `yotube_video_id` が null のセグメントは「動画なし」と表示する

> **根拠**
>
> **外部リンクにする理由**: 埋め込みだと YouTube の Branding Guidelines 遵守が必要になる。またiPhone では YouTube アプリに飛ばした方が快適。リンクなら制約がほぼない。

### 10.6 画面仕様

**先行事例 `no-ri/conan-index-site` と同じ構成にする。** 単一ページ、タブ切り替え、クライアントサイド完結。

#### 共通

```
[非公式ファンサイトである旨のバナー]        ← 常時表示（§12.3）
[ 図鑑 | アニメ ]                          ← タブ切り替え
[検索ボックス]  [検索対象の切り替え]
[絞り込みパネル（折りたたみ）]
[並び替え]
[結果リスト]
[フッター: クレジット・出典・連絡先]        ← §12.1
```

#### 図鑑タブ

```
検索対象:  日本語名 | 英語名 | 両方
絞り込み:  §9.1 の軸（種族/ランク/属性/役割/色/動物種/好物/嫌いな物/
                     初登場作品/ボス/レア/ファミリー）
並び替え:  図鑑番号順 | 名前順 | ランク順 | ランダム
結果:      妖怪カード（日本語名・英語名・種族・ランク・属性を色と図形で表現）
           タップで詳細（登場した回の一覧へリンク）
           所持メダルのチェックボックス
```

#### アニメタブ

```
検索対象:  サブタイトル | 登場妖怪 | 登場人物 | すべて
絞り込み:  シリーズ（初代 / ♪）/ 放送年 / presence（メイン登場のみ等）
           未視聴のみ表示
並び替え:  新しい順 | 古い順 | ランダム
結果:      放送回カード（話数・放送日・日本語サブタイトル一覧）
           セグメントごとに ▶ 再生リンク（§10.5）
           出てくる妖怪・人物のタグ（タップで図鑑タブへ）
           視聴済みのチェックボックス
```

#### 視聴履歴・所持メダル

localStorage に保存し、以下を用意する（コナン版と同じ）。

```
[書き出す]           JSON をテキストエリアに出力
[クリップボードにコピー]
[読み込む]           貼り付けて復元
```

#### データなしの表示

`data_status` に応じてバッジを出す（§12.5）。「動画なし」「あらすじなし」「データなし（一覧のみ）」。

> **根拠**
>
> **コナン版と同じ構成にする理由**: 既に家族が使い慣れており、学習コストがゼロ。実装も参照できる。UI をゼロから設計する時間を、データ品質に回す方が価値が高い。
>
> **単一ページにする理由**: 静的サイトでルーティングを持つとホスティングの設定が要る。データ量が数MB程度なら全件をメモリに載せてクライアントで絞り込める。
>
> **書き出し・読み込みを用意する理由**: localStorage は端末・ブラウザごとに分離される。家族が複数端末で使うため、手動での移行手段が必要。

---

## 11. 検証ゲート（A項目）

### 11.1 位置づけ

以下の4項目は **実装希望機能** であり、実データによる実現可能性検証を通過した項目のみ実装する。

**A項目が全滅しても Phase 4 に進めること。第一弾は §9 のカテゴリ由来軸だけで成立する。依存関係を作ってはならない。**

### 11.2 項目と合格基準

| | 項目 | 材料 | 合格基準 | 実測 | 判定 |
|---|---|---|---|---|---|
| A1 | 日本語要約 | 妖Tube説明欄の `【...】` | **動画が対応するセグメント**の80%以上 | 未測定 | Phase 2.5 |
| A2 | 一言紹介 | `{{medallium}}` ＋ `Biology` | 95%以上 | **95.3%** | **通過** |
| A3 | 名前の由来 | `Etymology` 節 | 70%以上で50字超 | **94.3%** | **通過** |
| A4 | 検索キーワード | A1 と同じ | A1 に従属 | — | A1 次第 |

**A1 の分母は `yotube_video_id` が非 null のセグメント**とする。全セグメントではない。

参考値（実測）:

```
Fandom の全セグメント        1,186（初代 621 ＋ ♪ 565）
妖Tube の本編動画              696（初代 506 ＋ ♪ 190）
動画が存在する割合           58.7%   ← これは data_status で別途追跡する
```

不合格時の挙動:

- **A1**: 該当セグメントのみ「あらすじなし」表示。機能自体は残す
- **A2 / A3 / A4**: 項目ごと廃止（部分的に出すと不公平に見える）

### 11.3 材料不足時の挙動（構造的な強制）

```
1. 材料テキストを機械的に抽出する（LLM を通す前）
2. 抽出結果が閾値未満 → その時点で unknown 確定。LLM に投げない
3. LLM に渡すのは抽出済みテキストのみ。妖怪名・話数・作品名は渡さない
4. 出力に evidence（材料からの短い引用）を必須にする
5. evidence が材料テキストに含まれない → 出力を破棄して unknown
```

### 11.4 検証の成果物

**カバレッジレポート1枚**（項目別の材料充足率と閾値判定）。これを人間が見て、その場で実装可否を決める。

Gold set・混同行列は不要。見る指標は **unknown 率**と **evidence 検証の通過率**の2つのみ。

> **根拠**
>
> **ゲートを設けた理由**: 材料が不足しているのに LLM に生成させると、**内部知識で黙って埋める**。時間とリソースをロスした上に、誤ったデータが混入する。
>
> **手順3（名前を渡さない）が肝**: 「ジバニャンの名前の由来を説明して」と聞けば LLM は内部知識で答えてしまう。名前を伏せて材料だけ渡せば、材料になければ答えられない。コナン版の evidence 方式と同じ考え方だが、**投げる前に落とす**分こちらの方が強い。
>
> **A1 の分母を「動画があるセグメント」にした理由**: A1 が問うているのは「妖Tube の説明欄から日本語要約を生成できるか」であり、「全セグメントに動画があるか」ではない。動画がないセグメントは A1 の失敗ではなく対象外。動画の有無は `data_status` で別に追跡されており、分母に含めると同じことを二重に測ることになる。
>
> なお全1,186セグメントを分母にすると上限が 58.7% × 71% ≒ 42% となり、**基準80%は構造的に達成不能**になる。ゲートが常に不合格を返すだけで、判定として機能しない。
>
> **Gold set が不要な理由**: A項目はすべて「生成」タスクであり「判定」タスクではない。正解が一意に定まる分類ではないので、混同行列を作る意味がない。「原文を載せていない」「材料にない事実を書いていない」の2点だけ確認すればよい。
>
> **B・C項目（こわさ・性格タイプ・話のタイプ・主役等）を見送った理由**: 判定タスクなので Gold set と混同行列が必要になり、週9時間の予算に合わない。なお `Personality` 節は主役級2件にしか存在せず、性格軸はそもそも材料不足。

---

## 12. 表示ポリシー

### 12.1 クレジット（全ページ共通フッター）

```
本サイトは非公式のファンサイトです。
妖怪ウォッチは株式会社レベルファイブの商標です。
© LEVEL-5 Inc.

データ出典:
  Yo-kai Watch Wiki (Fandom) — CC BY-SA
  ウィキペディア日本語版 — CC BY-SA 4.0
  妖怪ウォッチ公式YouTubeチャンネル「妖Tube」

本サイトの生成データは CC BY-SA で提供します。
ご指摘・削除依頼は {連絡先} まで。
```

各妖怪ページ・各話ページには、**出典元への個別リンク**も置く。

> **根拠**
>
> CC BY-SA の帰属要件は「各ページで」が原則。フッター一括だけでは不十分。
>
> **連絡先を置く理由**: 権利者が削除を求めたいとき、連絡手段がないと**いきなり GitHub への DMCA 通報**という経路になる。連絡先があれば穏当なメールが先に来て、話し合いで済む。GitHub Issues へのリンクでも足りる。

### 12.2 ライセンスの分離

```
LICENSE                   MIT        ← コード
docs/data/LICENSE         CC BY-SA   ← 生成データ
docs/data/ATTRIBUTION.md             ← 出典の詳細と取得日
```

`raw/` と `reports/` は `.gitignore` の対象であり、`docs/data/LICENSE` の適用範囲外。

> **根拠**
>
> 生成データが Fandom 由来である以上、ShareAlike が及ぶ。**public リポジトリで出すこと自体が義務の履行になる**ので、これは手間ではなく利点。

### 12.3 名称と検索エンジン

商標は**裸で使わず、必ず修飾語とセットで使う**。

```
×  妖怪ウォッチ / Yo-kai Watch      裸の商標
×  妖怪ウォッチ公式 / -official     公式の僭称
×  yokai-watch-jp                   公式の地域版に見える
○  yokai-watch-index                索引であることを明示
○  妖怪ウォッチ アニメ・妖怪検索（非公式ファンサイト）
```

- リポジトリ名: `yokai-watch-index`
- `<title>`: `yokai-watch-index — 妖怪ウォッチ アニメ・妖怪検索（非公式ファンサイト）`
- 第一弾は `<meta name="robots" content="noindex, nofollow">` と `robots.txt` の `Disallow: /`
- ただし**将来の index 開放を前提とした表記を最初から実装する**

名称より重要な「公式に見えないための措置」:

- **ロゴフォント・公式配色を模倣しない**
- 非公式である旨をフッターだけでなく**ページ上部**にも置く
- 連絡先を用意する
- **OGP 画像を作らない**（SNS で公式風に見えるため）

> **根拠**
>
> **裸か修飾つきかで評価が変わる**: 商標を出所表示として使う（商標的使用）と、対象を指し示すために使う（記述的使用）では法的評価が異なる。`yokai-watch-index` は「妖怪ウォッチの索引」を意味し、後者にあたる。当初は「名称に商標を冠さない」としていたが、`<title>` では既に記述的に使っており、実際のルールは「裸で使わない」であると整理し直した。
>
> **名称より見た目が重要**: 不正競争防止法の混同惹起は、需要者が公式と誤認するかで判断される。ロゴや配色の模倣の方が、リポジトリ名より直接的に効く。
>
> **リポジトリ名の変更は容易**: GitHub は旧 URL からリダイレクトを張るため、後から変えてもリンクが切れない。ここで過度に悩む実益は小さい。
>
> **法的義務は index の有無で変わらない**: CC BY-SA の帰属・ShareAlike、原文非表示、プライバシーポリシー、画像ゼロは、公開した時点ですべて発生済み。noindex は法的要件ではなく単なるリスク低減策。したがって「後から検索に出す」は `<meta>` 1行と `robots.txt` の削除だけで済む。
>
> **第一弾を noindex にする理由は権利面ではなく品質**: データの誤りや突合の失敗は必ず残るので、まず自分と家族で使って穴を潰してから開ける。

### 12.4 プライバシーポリシー

1ページ用意する（YouTube API の要件）。内容:

- 視聴履歴・所持メダルはブラウザの localStorage にのみ保存し、サーバーに送信しない
- アクセス解析を入れない（GitHub Pages 標準のログのみ）
- YouTube へのリンク先では Google のプライバシーポリシーが適用される

### 12.5 「データなし」の明示

`data_status` を UI に必ず出す。

```
未配信セグメント     → 「動画なし」
あらすじ未取得       → 「あらすじなし」
劇場版               → 「データなし（一覧のみ）」
```

> **根拠**
>
> 空白を隠さない。何が欠けているかが見えていれば、子どもが「壊れている」と感じずに済むし、後で埋める場所も分かる。

---

## 13. 運用

### 13.1 妖Tube の増分更新

妖Tube は**現在も稼働中**で、初代の配信も進行中（欠番114個は「まだ上がっていない」分を含む可能性が高い）。

月1回、全件を取り直す。クォータ消費は約50ユニットなので問題ない。

GitHub Actions での定期実行を推奨。API キーは Secrets に置く。

### 13.2 リンク切れの検出

動画が非公開になる可能性があるため、再取得時に生存確認し、消えたものは `available: false` を立てる。

### 13.3 Fandom データの更新

**自動化できない**（§2.3）。人間が半年〜1年に1回、`Special:Export` で取り直す。手順は §5.1。

### 13.4 API キーの管理

- `.env` に置き、`.gitignore` に `.env` を追加
- GitHub Actions では Secrets を使う
- API キーは YouTube Data API v3 のみに制限をかける

---

## 14. 実装フェーズ

```
Phase 0   事実確認                      ✅ 完了（§15）
Phase 1   設計判断                      ✅ 完了（§16）
Phase 2   取得パイプライン               ← 次はここ
Phase 2.5 A項目 実現可能性検証            ← ゲート（§11）
Phase 3   A項目の生成（通過分のみ）    ← **第一弾では実施しない**
Phase 4   フロントエンド・公開
```

**第一弾のスコープは Phase 2 → Phase 2.5 → Phase 4。**
A2 / A3 はゲートを通過しているが、生成そのものは第一弾に含めない。
`intro_ja` / `etymology_ja` / `synopsis_ja` / `keywords` はスキーマだけ用意し、すべて `null` とする。

> **根拠**
>
> §11.1 が「A項目が全滅しても Phase 4 に進めること。依存関係を作ってはならない」と定めており、null のまま先に進める設計になっている。§4.1 / §4.4 で nullable として定義済みなので、後から追加してもスキーマが壊れない。
>
> 3,414件 × 2種類 = 6,828本の生成は、実行手段・バッチ構成・コストの設計が別途必要で、第一弾を通しで走らせる妨げになる。まず動くものを出し、実物を見てから判断する。

### 14.1 Phase 2 のタスク

```
2-1  Fandom XML パーサ（infobox + カテゴリのマージ）
2-2  自己検証レポートの出力（§5.13）
2-3  （欠番。ja.Wikipedia は第一弾では使わない — §6）
2-4  YouTube Data API 取得（メタデータは一時保持）
2-5  文字列正規化と3段突合
2-6  unmatched.csv の出力
2-7  カバレッジ計測レポート（シリーズ別・軸別）
```

### 14.2 Phase 2 の前提（人間の作業）

- ~~Fandom から `Special:Export` で全ページを取得~~ **完了**（2026-08-15、13ファイル、4,364ページ）
- ~~YouTube API キーの発行~~ **完了**
- ~~`overrides/unresolved_yokai.csv` の作成~~ **完了**（82件、うち解決20件）

**人間側の作業は完了している。Phase 2 は Claude Code が単独で着手できる。**

### 14.3 リポジトリ構成

```
yokai-watch-index/
├── CLAUDE.md                    Claude Code への入口指示
├── SPEC.md                      本仕様
├── README.md
├── LICENSE                      MIT（コード）
├── .gitignore
├── .env.example
├── sources.yaml                 §3.2
│
├── raw/                         ★.gitignore — 取得元の原文
│   ├── fandom/*.xml             [準備済 13ファイル]
│   ├── fandom/titles.txt        [準備済 4,364行] §5.13 の照合先
│   ├── fandom/MANIFEST.txt      [準備済] 取得記録
│   └── youtube/
│
├── overrides/                   人間が書く対応表（パイプラインの入力）
│   ├── unresolved_yokai.csv     [準備済 82件] §5.10
│   ├── movies.csv               劇場版の手入力 §5.15
│   └── segment_youtube_map.csv  §8.2
│
├── tools/                       Phase 0 の調査スクリプト [準備済]
│   ├── check_fandom_export.py   §15 の実測値の出典
│   ├── extract_page_list.py
│   ├── make_overrides.py
│   └── yotube_inventory.py
│
├── scripts/                     Phase 2 以降の本番パイプライン
│   ├── parse_fandom.py
│   ├── fetch_jawiki.py
│   ├── fetch_youtube.py
│   ├── match_segments.py
│   └── build_data.py            docs/data/*.json を生成
│
├── reports/                     ★.gitignore — 検証出力
│   ├── unknown_params.csv
│   ├── conflicts.csv
│   ├── unresolved_yokai.csv
│   ├── unknown_presence.csv
│   ├── segment_title_mismatch.csv
│   └── unmatched.csv
│
├── tests/
│
└── docs/                        ★GitHub Pages の公開ルート。コミットする
    ├── index.html
    ├── app.js / style.css
    ├── manifest.json            PWA マニフェスト（§10.3）
    ├── apple-touch-icon.png
    ├── robots.txt
    ├── privacy.html             §12.4
    └── data/                    CC BY-SA
        ├── LICENSE
        ├── ATTRIBUTION.md
        ├── yokai.json
        ├── characters.json
        ├── episodes.json
        ├── segments.json
        └── facets.json          絞り込み軸の対応表 §9.1
```

**GitHub Pages の設定**: Source = "Deploy from a branch"、Branch = `main`、Folder = `/docs`。

データフローは **`raw/` → `scripts/` → `docs/data/` → `docs/` の一方向**。
`docs/` が `raw/` を読むことはない。

`docs/data/*.json` は**リポジトリにコミットする**（GitHub Actions でのビルドは行わない）。

> **根拠**
>
> **`docs/` を公開ルートにした理由**: GitHub Pages をブランチから配信する場合、ソースフォルダに選べるのは**リポジトリのルート（/）か `/docs` のみ**で、`site/` のような任意名のフォルダは指定できない。`site/` を使うには GitHub Actions のワークフローが必要になる。CI を組まずに `git push` だけで公開できることを優先し、`/docs` を採用した。
>
> **`data/` を `docs/` の中に入れた理由**: `/docs` を配信元にすると、**`docs/` 配下のファイルしか配信されない**。フロントが `data/*.json` を fetch するには、データも `docs/` の中に置く必要がある。
>
> **`docs/overview.md` を作らない理由**: `docs/` がサイトの公開ルートになったため、開発ドキュメントを同居させられない。また本仕様が構造図（本節）を含んでおり、`CLAUDE.md`（入口）+ `SPEC.md`（本体）の2階層で足りる。二重管理を避ける。
>
> **`raw/` と `reports/` をトップレベルに置いた理由**: `docs/data/LICENSE` は CC BY-SA を宣言する。非公開の原文を同じ階層に置くと、ライセンス表記の適用範囲が曖昧になる。分離することで、`.gitignore` を見るだけで公開・非公開の境界が判別できる。
>
> **`scripts/` と `docs/` を分けた理由**: YouTube API を叩くのは `scripts/` だけ、`docs/` は生成済み JSON を読むだけ。**API キーが公開ルートに紛れ込む経路が構造的に存在しない**（§10.1）。`/docs` 配信なら `scripts/` や `raw/` はそもそも配信対象外。
>
> **`overrides/` を入力として扱う理由**: `reports/` に出た失敗リストを人間が見て `overrides/` に手で書く。パイプラインを何度再実行しても手作業が失われない構造にするため。
>
> **`tools/` を残す理由**: §15 の実測値がどう算出されたかの出典になる。数値を疑ったときに再現できる。
>
> **JSON をコミットする理由**: GitHub Pages がそのまま配信でき、CI の設定が不要。CC BY-SA の ShareAlike も public リポジトリに置くことで自動的に満たされる。データ量は数MB程度と見込まれ、リポジトリサイズの問題にならない。

---

## 15. 検証済みの事実（Phase 0 の計測結果）

**この章の数値はすべて実データから測定したもので、推測ではない。**

### 15.1 Fandom（yokaiwatch.fandom.com）

取得完了日: 2026-08-15。エクスポート13ファイル、**titles.txt で 4,364 ページ**。

```
wiki 全体のページ数   6,212
MediaWiki             1.43.9
ライセンス表示         CC-BY-SA（テキストのみ）
robots.txt            Special:* を拒否
```

**取得できたページの内訳**（パーサが認識した4,330件ベース。34件は §5.13 のバグで未認識）:

| 種別 | 件数 | 判定に使ったテンプレート |
|---|---|---|
| 妖怪 | **3,414** | `{{yo-kai}}` / `{{yokai}}` |
| 人間キャラ | **160** | `{{character}}` |
| エピソード | **459** | `{{episode}}` / ID形式 |
| リダイレクト | **251** | `#REDIRECT` |
| unclassified | 86 | 一覧ページ、`Brave (tribe)` 等の概念ページ、名前空間つきページ |

XML 内の `<page>` は 4,369 件。うち4タイトルが重複エクスポートされており、
重複排除すると `titles.txt` の 4,364 と**集合が完全一致**する（両方向の差ゼロ）。
本文が空で `<text ... />` と自己閉じになっているページが34件ある（§5.13）。

**エピソード（全シリーズ欠番ゼロ）**:

| prefix パラメータ | 件数 | 内訳 |
|---|---|---|
| （なし） | 216 | 初代 EP001-214（**214**）＋ EX011 / EX012（2） |
| `MN` | **96** | ♪ MN001-096 |
| `SS` | 49 | シャドウサイド |
| `YSH` | 64 | Y学園（ページ名は `YG001` 形式） |
| `EX` | 34 | 妖怪ウォッチ! と推定 |
| 計 | **459** | |

**劇場版（`prefix = M`）は0件。** export に1ページも含まれていない（§5.15）。

**日本語名と材料の充足率**:

| 対象 | 実測 |
|---|---|
| 妖怪の日本語名 | **3,414 / 3,414 = 100.0%** |
| うち `{{language}}` の `ja-name` 経由 | 783 / 3,414 = 22.9%（残りは infobox 経由） |
| 人間キャラの日本語名 | 145 / 154 = 94.2% |
| `Etymology` 節 | 3,219 / 3,414 = **94.3%** |
| `{{medallium}}` | 3,253 / 3,414 = **95.3%** |
| `Biology` 節 | 3,399 / 3,414 = 99.6% |
| `{{episode}}` の `japan`（放送日） | **310 / 310 = 100%**（初代＋♪） |

**infobox パラメータ: 140種類**（§5.5 のエイリアス表を参照）。

**確認済みの `{{episode}}` パラメータ**:
```
number / prefix / image / episode title
japan / america
j-opening / en-opening / j-ending / en-ending
animation / screenplay / storyboard / director / amindirector
```

**確認済みのカテゴリ軸**:
```
種族      Charming Tribe / Tough Tribe / Brave Tribe / Shady Tribe /
          Mysterious Tribe / Heartful Tribe / Eerie Tribe / Slippery Tribe
          Mononoke Tribe / Oni Tribe / Uwanosora Tribe / Omamori Tribe
ランク    Rank E〜S Yo-kai
属性      Fire-attribute Yo-kai / Drain-attribute Yo-kai ...
役割      Fighter / Tank / Ranger Role Yo-kai
色        Red / Blue / White / Black / Purple / Pink Yo-kai
生き物    Cat / Dog / Oni / Humanoid / Animal Yo-kai / Komainu
特殊      Boss Yo-kai / Rare Yo-kai / Present Yo-kai / Befriendable Boss Yo-kai
好物      Yo-kai That Love Milk / Candy / Meat / Chocobars / Seafood / Rice Balls
嫌い      Yo-kai That Despise Vegetables / Milk / Juice
入手      Yo-kai Befriended During Story (YW2) / (YW3) / (YWB)
作品限定  Yo-kai Exclusive To Busters 2: Magnum など
派生      Jibanyan's Variants
```

**散文セクションの存在率**（サンプル5件、§11 の材料判断用）:

| ページ | Etymology | Personality | Biology | Medallium |
|---|---|---|---|---|
| Jibanyan | 1019字 | 2121字 | 2673字 | ○ |
| Komasan | 700字 | 1623字 | 3062字 | ○ |
| Orcanos | 546字 | **0** | 1944字 | ○ |
| Leadoni | 712字 | **0** | 1041字 | ○ |
| Pandle | 1019字 | **0** | 452字 | ○ |

`Personality` は主役級にしか存在しない。

### 15.2 妖Tube（2026-08-14 時点）

```
総動画数              1,811
投稿期間              2016-07-06 〜 2026-08-14（現在も継続中）
#shorts               406
「まとめ」            154
本編候補              858（【公式】かつ shorts でもまとめでもない）
あらすじブロックあり  609 / 858 = 71%
```

シリーズ別:

| シリーズ | 本数 | 番号範囲 | 欠番 |
|---|---|---|---|
| 初代 | 506 | #2〜621 | 114個 |
| 妖怪ウォッチ♪ | 190 | #1〜190 | **0** |
| 妖怪ウォッチ! | 12 | — | — |
| シャドウサイド | 17 | — | — |

動画長の分布:
```
20分以上   139本
10-20分    141本
5-10分     629本
1-5分      359本
1分未満    543本
```

### 15.3 突合の実例

```
Fandom EP001 : 妖怪がいる!   （半角 !）
妖Tube #1    : 妖怪がいる！  （全角 ！）
```

```
妖Tube タイトル   : 妖怪しょうぶし
妖Tube 説明欄     : 【妖怪しょうブシ】
```

```
リンク側   : 'Konbu-san\u00a0(Shadowside)'   （U+00A0 ノーブレークスペース）
ページ名   : 'Konbu-san (Shadowside)'        （U+0020）
```

### 15.4 発見されたデータ誤り

**① `{{translation}}` の第3引数に別の話のデータが混入**（EP151）

```
{{translation|'''Tomnyan's Japon Exploration: First New Year'''
|トムニャンのジャポン探訪『はじめてのお正月』
|Jibanyan to itsutsu no purezento da nyan!}}
```

**② カテゴリの衝突**

```
EP001（初代 第1話）: Season 1 episodes / Season 1 Dub episodes / Episodes
MN001（♪ 第1話）   : Episodes / Yo-kai Watch♪ episodes / Season 1 episodes
```

**③ infobox パラメータの誤記**（5種類）

```
attribue / triobe / romanji name / apaneseva / foosyw3
```

**④ 本文リンクの綴り誤り**

```
Dismerelda → Dismarelda / Happiere → Happierre / Dummkapp → Dummkap
Roughraf → Roughraff / Susumu Goto → Susumu Gotou
Chuu Un → Chou Un（6件）/ Kyubi → Kyuubi
Koma Kaachan (Y-Aacademy) → Koma Kaachan (Y-Academy)
```

**このwikiにはデータ誤りが実在する**という前提で設計すること。

### 15.5 未解決リンクの内訳（82件）

| confidence | 件数 | 内容 |
|---|---|---|
| `high` | **20** | 空白差異・綴り誤り。`overrides/unresolved_yokai.csv` で解決済み |
| `anime_only` | 10 | 個別ページなし。`List of Anime-only Yo-kai` に記載 |
| `none` | 52 | 解決不能（赤リンク、または別個体の可能性があり断定不可） |

追加取得を試みても取れないことを確認済み。

### 15.6 セグメント数と presence 注記（着手前レビューでの追加計測）

**セグメント数**（`{{episode}}` の lead 段落にある `{{translation}}` の個数）:

| シリーズ | 放送回 | Fandom セグメント | 1話あたり | 妖Tube 本数 |
|---|---|---|---|---|
| 初代 EP | 214 | **621** | 2.90 | 506（#2〜621、欠番114） |
| ♪ MN | 96 | **565** | **5.89** | 190（欠番0） |
| 計 | 310 | **1,186** | — | **696** |

初代は621と `#2〜621` が完全整合する。♪の5.89は定期ミニコーナーを含むため（§5.14）。

`episode title` パラメータとの個数不一致は3件（EP031・EP063 はパラメータ自体が無い、EP146 は3対4）。

**presence 注記は実測65種類**（初代＋♪の全310話）。主なもの:

| 注記 | 件数 |
|---|---|
| `(cameo)` | 1,240 |
| **`(debut)`** | **410** |
| `(non-speaking cameo)` | 68 |
| `(mentioned)` | 54 |
| `(cameo; pictured)` | 39 |
| `(medal only)` | 36 |
| `(flashback)` | 21 |
| `(cameo; medal)` | 21 |
| `(debut, cameo)` | 16 |
| `(shadow cameo)` | 15 |
| その他55種類 | 各1〜13 |

区切り文字は `;` と `,` が混在する。処理規則は §5.8。

---

## 16. 決定履歴

| # | 決定 | 日付 |
|---|---|---|
| D-1 | Fandom ページIDが主キー。episodes（放送回）/ segments（小話）の2層。劇場版は一覧のみ | 2026-08-15 |
| D-2 | 対象は初代（EP001-214）＋♪（MN001-096）。SS/YG はスキーマのみ用意 | 2026-08-15 |
| D-3 | 1妖怪1レコード。`(anime)` は剥がして統合。`family` で派生を束ねる | 2026-08-15 |
| D-4 | `tribe` / `tribe_ss` を分離。カテゴリ優先＋infobox補完、衝突は記録 | 2026-08-15 |
| D-5 | 第一弾はカテゴリ由来の軸のみ。A項目は §11 のゲート通過を条件とする。B・C項目は見送り | 2026-08-15 |
| D-6 | 原文非表示・画像ゼロ・CC BY-SA表記・第一弾は noindex・localStorage のみ | 2026-08-15 |
| D-7 | 公開範囲は GitHub Pages（public）。将来の index 開放を前提とした表記を最初から実装 | 2026-08-15 |
| D-8 | 広告・アフィリエイトを設置しない | 2026-08-15 |
| D-9 | 人間キャラクターを独立テーブル `characters` にする（`{{character}}` 発見による） | 2026-08-15 |
| D-10 | リポジトリ名は `yokai-watch-index`。商標は裸で使わず修飾語とセットで使う（§12.3） | 2026-08-15 |
| D-11 | 単一リポジトリ。GitHub Pages は `/docs` から配信し、データも `docs/data/` に置く。`raw/` `reports/` はトップレベルで gitignore。`docs/overview.md` は作らない（§14.3） | 2026-08-15 |
| D-12 | A1 ゲートの分母は「動画が対応するセグメント」。全セグメントではない（§11.2） | 2026-08-15 |
| D-13 | **Phase 3（A項目の生成）は第一弾に含めない。** A2/A3 はゲート通過済みだが、生成は後回し（§14） | 2026-08-15 |
| D-14 | ja.Wikipedia は第一弾で使わない。`air_date` / `staff` は Fandom から取得、`title_ja_full` は null（§6） | 2026-08-15 |
| D-15 | 劇場版は `overrides/movies.csv` に手入力。Fandom の export に存在しないため（§5.15） | 2026-08-15 |
| D-16 | `presence` は `is_debut` を分離し、未知の注記は `main` に倒してレポート出力する（§5.8） | 2026-08-15 |
| D-17 | 絞り込み軸は `categories` から導出。`docs/data/facets.json` に対応表を持つ（§9.1） | 2026-08-15 |

### 16.1 検討したが採用しなかった案

| 案 | 不採用の理由 |
|---|---|
| 攻略サイト（Game8 等）からのデータ取得 | 全社が規約で無断転載・自動収集を禁止 |
| テレビ東京公式のあらすじ | 著作物、API なし、複製禁止 |
| Fandom API / スクレイピング | 利用規約が自動アクセスを禁止 |
| Fandom のカテゴリ分割による全件取得 | カテゴリを持たない妖怪が漏れる。一覧ページからの逆算に変更 |
| ゲームのステータス数値 | wiki 内で正確性に疑義。子ども向けに不要 |
| 妖Tube の `#N` を主キー | 未配信セグメント（114個）がキーを持てない |
| 妖怪のアニメ版/ゲーム版を別レコード | 検索結果に同じ妖怪が2回出る。相互参照が目的なので1ID1妖怪が確実 |
| `(Shadowside)` サフィックスの除去 | 別作品の別キャラクター。剥がすと解決に失敗する |
| 未解決リンクの自動曖昧一致 | `Sighborg S` → `Sighborg Y` のように別個体へ誤爆する |
| LLM による分類軸（B・C項目） | 検証工程（Gold set・混同行列）が重く、週9時間の予算に合わない。後から追加可能 |
| Cloudflare Pages + Access | 手軽さを優先し GitHub Pages を選択。移行は後からでも容易 |
| サイトを別リポジトリに分離（コナン版の方式） | `raw/` は gitignore 済みで漏洩リスクがなく、分ける実益がない。CC BY-SA の帰属が分散し、更新のたびに2リポジトリを同期する手間が生じる |
| GitHub Actions で `site/` を配信 | ワークフローの保守が増える。`/docs` 配信なら CI ゼロで済む |
| 埋め込み YouTube プレイヤー | Branding Guidelines 遵守が必要。iPhone では外部リンクの方が快適 |

---

## 17. 未解決事項

| # | 内容 | 解決タイミング |
|---|---|---|
| U-1 | 劇場版の日本語タイトル | **解決**。`overrides/movies.csv` に人間が手入力（§5.15） |
| U-2 | 初代＋♪に絞ったときのあらすじ充足率。71% は全シリーズ込みの数値 | Phase 2.5（A1 ゲート） |
| U-3 | `List of Anime-only Yo-kai` の中身。個別ページを持たない妖怪10件が一覧内に記述されている | 第一弾では対象外 |
| U-4 | `presence` の注記パターンの網羅 | **解決**。実測65種類（§15.6）。処理規則は §5.8 |
| U-5 | パーサが認識できていない34ページの正体 | **解決**。本文が空で `<text ... />` 自己閉じになっているページ（§5.13） |
| U-6 | 連絡先をどうするか（GitHub Issues か専用アドレスか） | Phase 4 |
| U-7 | `EX` 系列34件の正体（『妖怪ウォッチ!』と推定） | 第一弾ではスコープ外 |
| U-8 | ♪の定期ミニコーナーの網羅。`is_recurring` の閾値5話が妥当か | Phase 2 で実測して調整 |
