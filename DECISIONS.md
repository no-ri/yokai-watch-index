# 決定記録

`SPEC.md` に記述がない、または記述だけでは決まらなかった判断の記録。
INSTRUCTION.md「途中で迷ったときのルール」2 に基づく。

**「後戻りのコスト: 大」は必ず「要確認: はい」にする。**

---

## D-20260815-01  `{{yo-kai}}` と `{{character}}` の両方を持つページは妖怪として扱う

- 場面: PR2 / `scripts/parse_fandom.py` のページ分類
- 仕様の状況: §5.13 の種別分類に優先順位の記述がない
- 採用: `yokai` → `character` → `episode` の順で排他的に判定する（妖怪優先）
- 理由:
  実測で両方のテンプレートを持つページが6件ある。
  `Sandayu Koma` / `Koma Kaachan (Y-Academy)` / `Sun Sengumi` /
  `Summer Adams` / `Jinpei Jiba` / `Ryusuke Kyubi`。
  妖怪優先にすると内訳が 3,414 + 154 + 459 + 251 + 86 = 4,364 となり、
  `titles.txt` の行数と一致する。§15.1 の「人間キャラ 154」もこの前提の数値。
  キャラクター優先にすると合計が合わず、§15.1 と矛盾する。
- 他の案:
  ①キャラクター優先 → §15.1 の154と矛盾する
  ②両方に登録 → 1ページ1レコードの原則（D-3）が崩れ、検索結果に重複が出る
- 後戻りのコスト: 小（6件のみ。再分類は容易）
- 要確認: いいえ

**注**: SPEC.md 版2.1 の §15.1 は「人間キャラ **160**」に更新されているが、
これは着手前レビューでの計測誤り（`{{character}}` の有無を単独で数えたもの）。
排他分類では154が正しい。**§15.1 を154に戻す必要がある。**

---

## D-20260815-02  `episodes.prefix` は出力側の正規化値とする

- 場面: PR2 / `scripts/parse_fandom.py`
- 仕様の状況: §4.3 は `prefix EP|MN|M|SS|YG`、§5.11 の実測は `YSH`（`M` は存在しない）
- 採用: `prefix` には**正規化した値**（`EP` / `MN` / `M`）を入れる。
  Fandom の生の `prefix` パラメータ値はそのまま持たない
- 理由:
  §4.3 の enum は出力スキーマの定義であり、Fandom の生値の一覧ではない。
  初代は生値が空、劇場版は `overrides/movies.csv` 由来で Fandom に存在しないため、
  そもそも生値をそのまま入れることができない。
  シリーズの判別は §5.11 の二重チェックで行い、`series`（`gen1` / `uta`）が正となる。
  第一弾のスコープは `EP` / `MN` / `M` のみなので `SS` / `YG` は出力されない。
- 他の案: 生値をそのまま入れる → 初代が空文字になり、`series` と二重管理になる
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-03  `scripts/fetch_jawiki.py` は作成しない

- 場面: PR3
- 仕様の状況: §6 と §14.1 は「第一弾では使用しない」、§14.3 のツリーには残存
- 採用: ファイルを作成しない。PR3 は欠番
- 理由:
  §14.3 のツリーは版2.0 からの残りと判断した。
  `SPEC_ISSUES_ANSWER.md` §1 が「PR3 は欠番としてください」と明示している。
  空のファイルや未使用のスクリプトを置くと、後から「なぜ動かないのか」の調査対象になる。
- 他の案: 空ファイルを置く → 未使用コードが残るだけで利点がない
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-04  `title_ja_full` は常に null とする

- 場面: PR2 / PR6
- 仕様の状況: §4.3 に「（ja.Wikipedia 由来）」の但し書きが残っているが、§6 は不使用
- 採用: スキーマには残し、値は常に `null`。UI では表示しない
- 理由:
  §6 が第一弾での ja.Wikipedia 不使用を決めており、他に取得元がない。
  §9.2 の検索軸に含まれないため、欠けても機能は成立する。
  フィールドごと削るとスキーマ変更になるため、nullable のまま残す方が安い。
- 他の案: フィールドごと削除 → 後から足すときにスキーマ変更が必要
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-05  着手前レビューの記録は `files/` に残す

- 場面: PR1
- 仕様の状況: §14.3 のリポジトリ構成に該当する置き場がない
- 採用: `files/INSTRUCTION.md` / `files/SPEC_ISSUES.md` / `files/SPEC_ISSUES_ANSWER.md`
  をそのままコミットする
- 理由:
  CLAUDE.md は「開発ドキュメントは SPEC.md とこのファイルだけ」とするが、
  これは**仕様書を二重管理しない**ための規定であり、経緯の記録は対象外と解釈した。
  SPEC.md 版2.1 の多くの記述（§5.8 / §5.11 / §5.14 / §5.15）はこのレビューが根拠であり、
  「なぜこの規則になったか」を追えなくなる方が損失が大きい。
  `検討資料/` は旧版 SPEC.md を含むため gitignore（回答 §3 の指示どおり）。
- 他の案: gitignore する → §5.14 等の由来が追えなくなる
- 後戻りのコスト: 小
- 要確認: いいえ
