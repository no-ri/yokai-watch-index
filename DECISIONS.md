# 決定記録

`SPEC.md` に記述がない、または記述だけでは決まらなかった判断の記録。
INSTRUCTION.md「途中で迷ったときのルール」2 に基づく。

**「後戻りのコスト: 大」は必ず「要確認: はい」にする。**

---

## D-20260815-01  `{{yo-kai}}` と `{{character}}` の両方を持つページは妖怪として扱う

- 場面: PR2 / `scripts/parse_fandom.py` のページ分類
- 仕様の状況: §5.13 の種別分類に優先順位の記述がない
- 採用: `yokai` → `character` → `episode` の順で排他的に判定する（妖怪優先）
- 理由:
  実測で両方のテンプレートを持つページが6件ある。
  `Sandayu Koma` / `Koma Kaachan (Y-Academy)` / `Sun Sengumi` /
  `Summer Adams` / `Jinpei Jiba` / `Ryusuke Kyubi`。
  妖怪優先にすると内訳が 3,414 + 154 + 459 + 251 + 86 = 4,364 となり、
  `titles.txt` の行数と一致する。§15.1 の「人間キャラ 154」もこの前提の数値。
  キャラクター優先にすると合計が合わず、§15.1 と矛盾する。
- 他の案:
  ①キャラクター優先 → §15.1 の154と矛盾する
  ②両方に登録 → 1ページ1レコードの原則（D-3）が崩れ、検索結果に重複が出る
- 後戻りのコスト: 小（6件のみ。再分類は容易）
- 要確認: いいえ

**注**: SPEC.md 版2.1 の §15.1 は「人間キャラ **160**」に更新されているが、
これは着手前レビューでの計測誤り（`{{character}}` の有無を単独で数えたもの）。
排他分類では154が正しい。**§15.1 を154に戻す必要がある。**

---

## D-20260815-02  `episodes.prefix` は出力側の正規化値とする

- 場面: PR2 / `scripts/parse_fandom.py`
- 仕様の状況: §4.3 は `prefix EP|MN|M|SS|YG`、§5.11 の実測は `YSH`（`M` は存在しない）
- 採用: `prefix` には**正規化した値**（`EP` / `MN` / `M`）を入れる。
  Fandom の生の `prefix` パラメータ値はそのまま持たない
- 理由:
  §4.3 の enum は出力スキーマの定義であり、Fandom の生値の一覧ではない。
  初代は生値が空、劇場版は `overrides/movies.csv` 由来で Fandom に存在しないため、
  そもそも生値をそのまま入れることができない。
  シリーズの判別は §5.11 の二重チェックで行い、`series`（`gen1` / `uta`）が正となる。
  第一弾のスコープは `EP` / `MN` / `M` のみなので `SS` / `YG` は出力されない。
- 他の案: 生値をそのまま入れる → 初代が空文字になり、`series` と二重管理になる
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-03  `scripts/fetch_jawiki.py` は作成しない

- 場面: PR3
- 仕様の状況: §6 と §14.1 は「第一弾では使用しない」、§14.3 のツリーには残存
- 採用: ファイルを作成しない。PR3 は欠番
- 理由:
  §14.3 のツリーは版2.0 からの残りと判断した。
  `SPEC_ISSUES_ANSWER.md` §1 が「PR3 は欠番としてください」と明示している。
  空のファイルや未使用のスクリプトを置くと、後から「なぜ動かないのか」の調査対象になる。
- 他の案: 空ファイルを置く → 未使用コードが残るだけで利点がない
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-04  `title_ja_full` は常に null とする

- 場面: PR2 / PR6
- 仕様の状況: §4.3 に「（ja.Wikipedia 由来）」の但し書きが残っているが、§6 は不使用
- 採用: スキーマには残し、値は常に `null`。UI では表示しない
- 理由:
  §6 が第一弾での ja.Wikipedia 不使用を決めており、他に取得元がない。
  §9.2 の検索軸に含まれないため、欠けても機能は成立する。
  フィールドごと削るとスキーマ変更になるため、nullable のまま残す方が安い。
- 他の案: フィールドごと削除 → 後から足すときにスキーマ変更が必要
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-06  シリーズ判別はページ名を主、`prefix` を照合に使う

- 場面: PR2 / `scripts/parse_fandom.py` の `detect_series()`
- 仕様の状況: §5.11 は「ページ名が `MN\d{3}` **かつ** `prefix = MN`」を要求している
- 採用:
  ページ名のパターンで判定し、`prefix` は**存在するときだけ**照合する。
  矛盾した場合は `reports/conflicts.csv` に出して除外する
- 理由:
  **`{{episode}}` 本体に `prefix` を持たない MN 話が実在する**（例: MN090）。
  §5.11 をそのまま実装すると♪が 96 話中 64 話しか取れず、32話が静かに落ちる。
  §5.11 の目的は「`prefix` を持たない EX011 / EX012 を初代に混入させないこと」だが、
  これはページ名の判定だけで達成できる（`EX011` は `EP\d{3}` にマッチしない）。
  実装後の実測で EP 214 / MN 96、セグメント 621 / 565 となり、§15.6 と一致した。
- 補足:
  SPEC §5.11 と §15.1 の prefix 内訳（`(none) 216 / MN 96 / YSH 64 / EX 34`）は、
  ページ全体を検索して `{{episode/nav}}` の値を拾った数値だった。
  `{{episode}}` 本体のみで数えると `(none) 318 / MN 64 / YG 9 / EX 19 / SS 49` になる。
  **Y学園の infobox 側の値は `YSH` ではなく `YG`。**`YSH` は nav 側の値。
  両方の内訳は `tools/spec_audit.py` が出力する。
- 他の案:
  ①`prefix` 必須のまま → ♪が32話落ちる。仕様の目的に反する
  ②`{{episode/nav}}` の `prefix` を読む → nav は前後の話へのリンクであり、
    自分自身の属性ではない。MN001 の nav は `previous = YG063` を指しており、
    別シリーズの値が混ざる経路になる
- 後戻りのコスト: 中（シリーズ判定はすべての下流に効く）
- 要確認: **はい**

---

## D-20260815-07  presence 注記の区切りに `/` を加え、部分一致の受け皿を置く

- 場面: PR2 / `scripts/parse_fandom.py` の `fold_presence()`
- 仕様の状況: §5.8 は区切りを `;` と `,` と規定。未知は main ＋レポート
- 採用:
  区切りに `/` を追加し、完全一致しないトークンには
  キーワードの部分一致（`flashback` / `cameo` / `medal` / `costume` /
  `voice` / `picture` / `recap` / `silhouette` / `mentioned` 等）を試す。
  それでも該当しなければ §5.8 規則4 のとおり main ＋ `unknown_presence.csv`
- 理由:
  実データに `anime debut/non-speaking cameo` のようにスラッシュ区切りの注記がある。
  また `flashback only` `cameo medal` `whisper's costume` `recap only` のような
  自由記述が多く、完全一致だけでは14件が未知に落ちた。
  部分一致を入れると3件まで減り、残る3件（`original` / `human` / `program`）は
  実際に判断できないものだけになる。
  **レポート出力は残しているので、寄せた結果は後から見える。**
- 他の案: 完全一致のみ → 14件が main に倒れる。誤りではないが精度が落ちる
- 後戻りのコスト: 小（`unknown_presence.csv` で常に検証できる）
- 要確認: いいえ

---

## D-20260815-08  テストのフィクスチャは合成データにする

- 場面: PR2 / `tests/test_parser.py`
- 仕様の状況: CLAUDE.md は「パーサは実データの一部を fixture にする」
- 採用:
  実データの**構造上の癖**（自己閉じ `<text />`、`{{yokai}}` の表記ゆれ、
  ノーブレークスペース、ルビ HTML、`(cameo)` 等の注記、`<br >`）を再現した
  合成データを使う。取得元の散文は1文字も含めない
- 理由:
  このリポジトリは public なので、`tests/` に置いたものは公開される。
  Plot や Biology の散文を入れると §2.2（原文非表示）の趣旨に反する。
  一方テストの目的は**構造の癖に対する回帰**なので、散文は不要。
  再現している癖はすべて実データで確認済みで、出典は §15 と `tools/spec_audit.py`。
- 他の案: 実データの XML を切り出して置く → 公開リポジトリに原文が載る
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-09  中間データは `build/fandom.json` に置き、gitignore する

- 場面: PR2 / PR6
- 仕様の状況: §14.3 に中間生成物の置き場がない
- 採用: `build/` を新設して gitignore する
- 理由:
  `reports/` は人間が読む検証出力、`docs/data/` は公開物であり、
  パーサの出力（4.4 MB）はどちらでもない。
  `docs/data/` に直接書くと、禁止キー検査（§2.2）を通す前のデータが
  公開ルートに置かれる瞬間が生じる。`build/` を挟めば構造的に防げる。
- 他の案:
  ①`docs/data/` に直接出力 → 検査前のデータが公開ルートに入る
  ②`reports/` に混ぜる → 人間が読む出力と機械が読む出力が混在する
- 後戻りのコスト: 小
- 要確認: いいえ

---

## D-20260815-05  着手前レビューの記録は `files/` に残す

- 場面: PR1
- 仕様の状況: §14.3 のリポジトリ構成に該当する置き場がない
- 採用: `files/INSTRUCTION.md` / `files/SPEC_ISSUES.md` / `files/SPEC_ISSUES_ANSWER.md`
  をそのままコミットする
- 理由:
  CLAUDE.md は「開発ドキュメントは SPEC.md とこのファイルだけ」とするが、
  これは**仕様書を二重管理しない**ための規定であり、経緯の記録は対象外と解釈した。
  SPEC.md 版2.1 の多くの記述（§5.8 / §5.11 / §5.14 / §5.15）はこのレビューが根拠であり、
  「なぜこの規則になったか」を追えなくなる方が損失が大きい。
  `検討資料/` は旧版 SPEC.md を含むため gitignore（回答 §3 の指示どおり）。
- 他の案: gitignore する → §5.14 等の由来が追えなくなる
- 後戻りのコスト: 小
- 要確認: いいえ
